# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this project is

ATLAS is the CEO of the company: it plans, prioritizes, delegates work to MAYA and future digital assets, monitors execution and KPIs, allocates resources, produces executive reports, and continuously improves the business itself — autonomously by default, with a human approving only irreversible or high-risk actions (large financial commitments, legal agreements, privileged-access changes, destructive operations, and any redesign of the business's own workflows/agents/processes).

Two permanent layers:
- **`atlas.core`** — the asset registry. Adding a new asset — of any kind — never requires modifying core code, and "asset" doesn't presuppose a specific lifecycle (a business or tool may have no running process at all; an agent or workflow might).
- **`atlas.brain`** — the CEO decision-making layer that plans, risk-gates, delegates, monitors, and reports, driving `atlas.core` from above. `atlas.core` never depends on `atlas.brain`.

MAYA is Asset #1. New assets are not created automatically by the brain — where it identifies a capability gap (e.g. no marketing asset exists yet), it produces a `Proposal` for a human to act on, never a file it writes itself.

**Standing architectural principle (2026-08-09, founder directive): ATLAS first, companies after.** Every new capability ATLAS gains from here on (CRM, Marketing, Affiliate, Finance, Content, Sales, Customer Management, Analytics, or any future business function) is built at the ATLAS platform layer — reusable by every company ATLAS goes on to operate — never built specifically for one company first. Maya Health, ATLAS's first real company (see the Maya Health mission documents referenced from `HANDOFF.md`), is a deliberate proof case for this, not an exception to it: her own Day-1 build is paused, by explicit founder instruction, until ATLAS's own unified operator interface exists for her to be built *from* — a real "command room" surfacing every company, every research finding, every asset, every campaign, every decision, every learned pattern, every memory, and every business action ATLAS takes, across all companies at once. This extends, at the interface layer, the same discipline `atlas.core`/`atlas.brain` already enforce at the code layer above (never company-specific, never asset-specific). `atlas.repl`/`atlas.app`/`atlas.brain.console` are real today but only surface a partial view (goals/tasks/per-asset reports/KPIs) — unifying them into the described command room, across multiple companies at once, is real, scoped future work, not yet built.

Six companion documents, each owning a different layer (see each file's own maintenance note): `CLAUDE.md` (this file) owns *how the code is structured*; `docs/ATLAS_BUSINESS_BLUEPRINT.md` owns *what the business does and why*; `HANDOFF.md` owns *current state of the build*, for session-to-session continuation; `docs/ATLAS_ARCHITECTURE_REFERENCE.md` owns *the engine layer's architecture, engine responsibilities, execution flow, and current implementation status* (Resource Discovery, Opportunity Discovery, Time Awareness, Decision Engine Integration, Business Execution Planning, and how they connect); `docs/NEW_BUSINESS_METHODOLOGY.md` owns *the mandatory 13-step process ATLAS follows before creating any new business, brand, Digital Influencer, or company* (demand → supply → competitors → regulation → affiliate market → products → niche comparison → objective ranking → selection → brand → business model → roadmap → execution), established after the Maya Health niche-selection missions proved a real anchoring-bias risk and fixed it with a standing counterfactual test ("would this conclusion hold without the pre-existing product?"); `docs/DEVELOPMENT_WORKFLOW.md` owns *how ATLAS itself works as a software engineer and as a CEO across every Mission* — the fixed phase sequence, when a Mission may be closed, when documentation/memory/commit/push are mandatory, and how rollback and long-term development history work — no Mission may be closed without passing every phase it defines. All six can go stale — cross-check against the code (`python -m pytest -q`, actual file contents) before trusting a specific claim, especially after a gap between sessions.

## Commands

- Install for development: `python -m pip install -e .`
- Running `atlas` with **no arguments** launches the full-screen operator app (`run_app()` in `src/atlas/app.py`) — this is the default entry point, not a CLI-only tool.
- Asset commands: `atlas list` / `atlas info <id>` / `atlas <verb> <id>` where `<verb>` is one of `start|stop|status|run|report`
- `atlas console` — one-shot consolidated operator view (goals, approvals, every department's `report()`, KPIs); same rendering the REPL's `status` command and the full-screen app's dashboard reuse.
- Brain commands: `atlas brain goal add "<description>" --priority N [--horizon short|long] [--expected-revenue N] [--required-investment N] [--time-to-first-profit N] [--scalability 0-1] [--automation-potential 0-1] [--strategic-value 0-1]`, `atlas brain goal list`, `atlas brain task add <goal_id> "<description>" [--category ...] [--amount N] [--reversible] [--privileged-access] [--legal-agreement]`, `atlas brain tick`, `atlas brain status`, `atlas brain approvals`, `atlas brain approve <task_id>` / `reject <task_id>`, `atlas brain proposals`, `atlas brain kpi record <name> <value>` / `kpi list`, `atlas brain report --period daily|weekly|monthly`, `atlas brain finding add <source> <category> "<description>" [--evidence <url>]` / `finding list` — records a real, sourced entry in the Intelligence knowledge base (`atlas/brain/knowledge.py`); `evidence` defaults to `""`, never a fabricated citation, `atlas brain opportunities [--explain]` — ranks every discovered category by evidence-weighted `confidence_score()`; `--explain` shows the full per-category evidence/confidence/expected-ROI/probability-of-success/risk breakdown (`atlas/brain/explain.py`), `atlas brain decisions list` / `decisions show <category>` — full Decision Engine traceability: verdict, confidence, deterministic reasoning, cited evidence Finding IDs, company context, risks, and what a decision superseded if it reopened a prior one (`atlas/brain/decisions.py`)
- Recruitment commands: `atlas recruitment demand add ...` / `supplier add ...` / `candidate add ...`, `atlas recruitment opportunities`, `atlas recruitment approve-outreach <id>` / `approve-commitment <id>` / `lost <id> [--reason ...]`
- Publishing commands: `atlas publishing queue` (list publish packages), `atlas publishing delete <package_id>` (direct housekeeping action, not approval-gated)
- Run all tests: `python -m pytest -q`
- Run a single test: `python -m pytest tests/brain/test_ceo.py::test_tick_delegates_a_new_goal_to_maya -q`
- **No CLI path exists to reactivate a paused `Goal`** — once the Strategist sets `Goal.status = "paused"`, only hand-editing `.atlas/brain.json` can undo it (open gap, see `HANDOFF.md`).

## Architecture: atlas.core (the asset registry)

**Assets are declared by a manifest, not by editing core code.** Each asset is a directory under `src/atlas/assets/<id>/` with a `manifest.toml` (`id`, `name`, `kind`, `description`, `owner`, `tags`, and an *optional* `entrypoint` of the form `module.path:ClassName`). `entrypoint` is optional on purpose — an affiliate business or marketing system may start as pure metadata with no code behind it yet. `kind` is an open string (`agent`, `business`, `marketing_system`, `workflow`, `tool`, `operational_agent`, `revenue_channel`, ...), not a closed enum.

- `src/atlas/core/loader.py` — `discover_manifests()` scans `*/manifest.toml` under the built-in assets dir plus any directories listed in `ATLAS_ASSETS_PATH`. Never imports asset code.
- `src/atlas/core/capabilities.py` — `Runnable` (`start`/`stop`/`status`), `Triggerable` (`run`), `Reportable` (`report`): `@runtime_checkable` `Protocol`s, not a shared base class.
- `src/atlas/core/registry.py` — `Registry` loads `AssetRecord`s eagerly, lazily imports/instantiates an asset's `entrypoint` on first use, and `dispatch(asset_id, verb)` capability-checks and calls it, raising `UnsupportedVerb` if unsupported.
- `src/atlas/core/store.py` — `Store` protocol + `JSONStore` (`.atlas/state.json`) — per-asset last-run state.

## Architecture: atlas.brain (the CEO)

- `src/atlas/brain/models.py` — `Goal` (durable objective; `horizon` — `"short"`/`"long"` — plus an optional `founder_estimate` six-criteria dict and `engine_id` feed the Strategist's capital-allocation scoring, see below), `Task` (one unit of planned work — `category` is an open string like `launch_campaign`/`analyze_revenue`/`redesign_workflow`; carries the four risk axes: `reversible`, `estimated_amount`, `involves_privileged_access`, `involves_legal_agreement`; `source_opportunity_id` is a correlation key set only by a brain-side "pipeline advance" bridge, see below — reused across several bridges to mean "what this dispatch is about", not always literally an opportunity), `Proposal` (a structural decision — create an asset, recruit an agent, or redesign something — that ATLAS cannot execute itself), `Finding` (a durable, channel-agnostic record of something the Intelligence layer discovered — see `knowledge.py`).
- `src/atlas/brain/memory.py` — `BrainMemory`, one JSON document (`.atlas/brain.json`, separate from the asset layer's `.atlas/state.json`) holding goals, tasks, proposals, KPI history, and an append-only decision/outcome log. Storage is delegated to an injectable `BrainStore` (`src/atlas/brain/store.py` — `JSONFileStore` by default, atomic writes, mirroring `atlas.core.store.Store`'s shape) so a future backend can be swapped in without changing `BrainMemory`'s API or any caller.
- `src/atlas/brain/knowledge.py` — `KnowledgeBase`, a separate JSON document (`.atlas/knowledge.json`) holding `Finding`s: a durable record of everything ATLAS's Intelligence layer has discovered, independent of whether a given finding ever became a `Task`/`Goal`. `Finding.category` is an open string (`affiliate`, `digital_product`, `youtube`, `ugc`, ...) — a channel with no dispatchable asset yet is still recorded, never dropped or force-fit into an existing category. `Finding.provider` is optional and orthogonal to category — `""` for a category-general finding, a registered provider name (e.g. `"digistore24"`) when the evidence is about that specific platform, one level deeper than category, which is what `provider_ranking.py` reads to rank platforms within a category. `evidence` defaults to `""`, never a fabricated citation. Reuses the same `BrainStore` abstraction as `BrainMemory`.
- `src/atlas/brain/risk.py` — `RiskPolicy.evaluate(task)`: **fail-closed**. A task must prove itself safe on every axis (reversible, within the amount threshold — default `0.0`, no privileged access, no legal agreement) *and* not be in `ALWAYS_REQUIRES_APPROVAL` (`create_asset`, `recruit_agent`) or start with `redesign_` to skip human approval. Unproven risk defaults to requiring approval, never the reverse. Every founder-approval gate added since (Affiliate Department, Affiliate Intelligence, Content Factory, Editorial Review, Publishing Gateway) reuses this exact mechanism via a plain `reversible=False` `Task` — none of them are a new gating system.
- `src/atlas/brain/planner.py` / `prioritizer.py` — `Planner`/`Prioritizer` protocols with one deterministic default each (`SimplePlanner`, `SimplePrioritizer`). Real, permanent infrastructure — not a stand-in for strategic reasoning. A smarter (e.g. LLM-backed) implementation swaps in behind the same protocol; nothing else in the brain changes. `SimplePlanner` marks its own generated tasks `reversible=True` (they're plain delegate/analyze/report work with no financial/access/legal component by construction) — this is what keeps routine work auto-delegating instead of piling up in approvals.
- `src/atlas/brain/delegator.py` — `Delegator.delegate(task, registry)`: routine tasks get matched to a `Triggerable` asset in the `Registry` and dispatched via `registry.dispatch(asset_id, "run", task=task)`, returning the raw dispatch result (`dict | None`) to the caller; if none match, the task is marked `blocked` (surfaced later as an opportunity/gap, not a silent failure). `is_structural(category)` tasks (gated categories from `risk.py`) always produce a `Proposal` instead — never a dispatch.
- `src/atlas/brain/monitor.py` — `Monitor.sync()` reads back each in-flight task's asset via `Reportable.report()` (falling back to "assumed complete" if the asset doesn't implement it), updates task status, appends an outcome-log entry, and refreshes the brain's own operational KPIs (`tasks_completed`, `tasks_blocked`, `pending_approvals`) — the substrate "learning from results" depends on.
- `src/atlas/brain/intake.py` — `absorb_opportunities()`: turns an asset's self-reported opportunities (e.g. Research's discoveries) into `Task`s under the right `Goal`, deduplicating so the same opportunity is never absorbed twice. Also records every opportunity as a `Finding` in the `KnowledgeBase` regardless of whether it becomes a `Task` — the durable side of intake, `Task` creation is the actionable side.
- `src/atlas/brain/kpi_intake.py` — `record_revenue(task, result, kpis, ledger=None)`: shape-dispatched, fail-closed attribution of a just-completed dispatch's revenue/cost to the correct goal. Revenue-channel shape (`"revenue_generated"` key) is attributed via `task.goal_id` and **accumulated** (one-off incremental amount per execution). Recruitment's shape (`"opportunities"` key) is attributed **only** via each opportunity's own stored `goal_id` (never the dispatching task's), counts only `stage == "won"` as realized, and is recorded as a **replacement reading** (full recomputation each time — idempotent, never inflates on repeated dispatch; the optional `ledger` only gets an entry for the actual delta against the last reading, never one per tick regardless of change). An unrecognized/malformed shape (Research's, or anything from the newer affiliate/content pipeline below) records nothing rather than guessing. Called from `CEOBrain._risk_gate_and_delegate()` and `CEOBrain.approve()`, right after `delegate()`, before any later same-tick dispatch can overwrite shared asset state. `record_manual_revenue(goal_id, amount, cost, kpis, ledger=None, ...)` and `record_manual_cost(goal_id, amount, kpis, ledger=None, kind="cost", ...)` (2026-08-03) are the founder-reported paths, both accumulate-semantics: the former for a real conversion (with optional per-conversion cost), the latter for real spend not tied to any single conversion (ad spend, subscriptions, setup cost, or a platform fee via `kind="fee"`) — added because affiliate content generation is verified zero-cost (deterministic templates, `content_factory/generator.py`, no LLM/paid API call), so the only real costs in the affiliate channel are founder-known spend with no automatic signal to derive them from. Wired via `atlas affiliate revenue record <package_id> <amount> [--cost N] [--provider] [--evidence] [--document]`, `atlas affiliate cost record`, and `atlas affiliate fee record` (same flags; all resolve `package_id` to `goal_id` via `PublishingQueueStore`). This closes the loop `confidence.py`'s `historical_success_score`/`measured_outcomes_score`/`internal_experiments_score` (50% of total weight) depend on — live-verified: recording cost against a goal with a real `affiliate_pipeline` task moves `atlas brain opportunities --explain` from 1/6 to 3/6 evidence factors and produces a real (non-`None`) Expected ROI.
  - `record_manual_settlement(goal_id, amount, kpis, ledger=None, ...)` and `record_manual_refund(goal_id, amount, kpis, ledger=None, ...)` (2026-08-03, Treasury layer, architecture locked) — settlement accumulates onto a new `settled_<goal_id>` series, deliberately separate from `revenue_<goal_id>`: **claimed revenue and settled cash are two different facts** (a claim can be pending, batched, delayed, or reversed before it's actually paid out), so collapsing them would hide a business that looks profitable on paper while being cash-poor in reality. Refund decrements `revenue_<goal_id>` directly (a real reversal of a claim that turned out wrong) rather than being written as a negative `revenue_claimed` ledger entry, so the Ledger reads as what actually happened. Wired via `atlas affiliate settlement record`/`atlas affiliate refund record` (same `package_id`/`--provider`/`--evidence`/`--document` shape). `cashflow.goal_cash_flow()` surfaces `settled` alongside `revenue` for visibility; `profit()`/`roi()` deliberately stay on the claimed basis `confidence_score()` already reads — settlement is a visibility layer on top, not a redefinition of profit.
- `src/atlas/brain/models.py` (`LedgerEntry`) / `src/atlas/brain/ledger.py` (`Ledger`) — the Finance & Accounting layer's foundation (2026-08-03, architecture locked): a durable, append-only detail/audit record beneath KPIRegistry's aggregate `revenue_<id>`/`cost_<id>`/`settled_<id>` series, one entry per real financial event (`kind`: `revenue_claimed` | `cash_settled` | `cost` | `fee` | `refund`), never mutated — a correction is a new entry, the same discipline `DecisionLog` already applies to Decisions. `provider` (which platform/account — Digistore24, Amazon, YouTube, TikTok, Shopify, Etsy, PayPal, Wise, Stripe, a bank account, or `""` for manual) is orthogonal to `kind`, the same relationship `Finding.provider` already has to `Finding.category` — adding a new platform never requires a new field or a new kind. `transaction_id` correlates entries belonging to one real-world transaction across its lifecycle when a real per-sale ID is known (a future provider integration); `""` for founder-reported entries today, since a payout is often a batch across multiple sales and inventing a one-to-one link would be a fabricated correlation. Purely additive: every existing `kpi_intake.py` function still does its original KPI-accumulate write unchanged; passing `ledger=` is optional and every prior call site/test that doesn't pass one behaves exactly as before. `CEOBrain.ledger` (defaults to `Ledger()`, same pattern as `knowledge`/`decisions`) is passed into both `record_revenue()` call sites so every tick-driven dispatch result is ledger-recorded too, not just founder-reported entries.
- **Pipeline-advance bridges** — one small brain-side module per multi-stage asset, all following the same shape first established for Recruitment: read the asset's `report()`, and for each goal-tagged item still in an internal (non-founder-gated) stage, ensure exactly one open "keep going" `Task` exists; once a stage needs a founder decision, create exactly one `reversible=False` task (never repeated for the same item). None of these are a generic framework — each is a small, asset-specific function, deliberately not unified into one engine:
  - `src/atlas/brain/pipeline_advance.py` — `advance_recruitment_pipeline()`, the original, for Recruitment's `discovered → qualified → matched` auto-advance (stops at the `proposal_ready`/`active` founder gates, and at `won`/`lost`).
  - `src/atlas/brain/affiliate_pipeline_advance.py` — `advance_affiliate_pipeline()`, for the Affiliate Department's `discovered → selected → content_planned` auto-advance, then one founder-approval task (with four *projected*, never-real `expected_*_<goal_id>`/`risk_score_<goal_id>` KPIs recorded alongside it — never written to the real `revenue_<goal_id>`/`cost_<goal_id>` series).
  - `src/atlas/brain/affiliate_intelligence_advance.py` — `advance_affiliate_intelligence()`, for Affiliate Intelligence's `discovered → researched → ranked` auto-advance, then one founder-*choice* task **per ranked opportunity** (the founder picks which one to pursue via the existing binary approve/reject, not a new multi-choice mechanism).
  - `src/atlas/brain/content_factory_advance.py` — `advance_content_factory()`: triggers first generation once an opportunity is `selected_for_marketing`, requests founder review once a package is `editorial_passed`, and turns a rejected review into "request changes" (regenerate once, then permanently abandon on a second rejection) — Approve/Reject/Request-changes expressed entirely through the existing binary `approve()`/`reject()` primitive, counted over time.
  - `src/atlas/brain/editorial_review_advance.py` — `advance_editorial_review()`: triggers evaluation once a package is unjudged, dispatches a fix request to Content Factory when revision is required (cross-asset, via the same category-tagged `Task` mechanism every dispatch already uses), and notifies the founder (informational, `reversible=False`) when a package is abandoned after exhausting revision cycles.
  - `src/atlas/brain/publishing_gateway_advance.py` — `advance_publishing_gateway()`: triggers package-building once content is `approved_for_marketing`, requests founder approval to queue a `READY` package, handles a rejection as cancellation, and records a full per-status queue-count snapshot (`publish_queue_<status>_<goal_id>`) every cycle, including zero counts (a status that drops to zero must be recorded as zero, or the last nonzero reading goes stale forever).
- **Intelligence Layer vs. Decision Engine** — a locked architectural boundary (2026-08-02), not an incidental pattern: the Intelligence Layer only ever discovers, verifies, and measures reality; it never recommends, prioritizes, or decides. The Decision Engine is the only component allowed to turn evidence into a business verdict, and never touches the governance boundary (RiskPolicy, `ALWAYS_REQUIRES_APPROVAL`, what requires founder approval) — it can conclude a new asset would be worth building, but `propose_capability` still only ever produces the same `create_asset` Task that's always required approval; it never creates one itself.
  - `src/atlas/brain/confidence.py` — Intelligence: `confidence_score(category, knowledge, memory, kpis)`, a weighted average of six named, evidence-derived factors (`source_corroboration`, `recency`, `repeatability` — always `None`, no real cross-market data exists — `historical_success`, `internal_experiments`, `measured_outcomes`), `WEIGHTS` a stated methodology not fabricated data. A missing factor is never treated as zero; zero available factors returns `None`. `CATEGORY_TASK_CATEGORIES` maps a `Finding.category` to the real, dispatchable Task categories (grepped from every manifest.toml, not guessed) that actually execute that channel — `youtube`/`ugc` map to `set()`, honestly, since no channel exists for either yet. `goals_touching_category()` is the real Task-category linkage every other module in this layer reuses rather than re-deriving. `weighted_average_of_available()` (the fail-closed combination step) and `rank_by_confidence()` (the None-safe, factors_available-tie-broken sort) are shared primitives, extracted (2026-08-02) after both were found independently duplicated in `provider_ranking.py`/`cli.py` — every confidence-based ranking in this codebase goes through these two, not a local reimplementation.
  - `src/atlas/brain/explain.py` — Intelligence: `explain_opportunity()`, the full per-category inspection view — evidence list, confidence breakdown, `expected_roi`/`probability_of_success` (both real or `None`, never guessed), concrete risks, why it ranked where it did. Read-only, on-demand; distinct from `Decision` (below), which is a committed, persisted verdict.
  - `src/atlas/brain/decision_engine.py` — Decision Engine: `decide(category, knowledge, memory, kpis)`, the only function in this codebase allowed to combine Intelligence's context-free `confidence_score()` with company context confidence never sees (existing commitments via `goals_touching_category()`, real execution capability via `CATEGORY_TASK_CATEGORIES`) into one of five verdicts: `insufficient_evidence` (`MIN_INDEPENDENT_SOURCES = 2` — a Decision Engine policy threshold, not an Intelligence fact), `already_invested`, `already_proposed`, `propose_capability`, `invest`. Pure and stateless — no caching, recomputes fresh every call, which is the entire mechanism behind "nothing is permanently true": calling `decide()` again after new evidence arrives *is* reopening the decision. `has_materially_changed()` is the anti-thrash check (`MATERIALITY_THRESHOLD = 0.02`) that stops `recency_score`'s continuous per-tick decay from logging a new Decision every cycle with no real evidence change behind it.
  - `src/atlas/brain/decision_apply.py` — `apply_decision(decision)`: executes exactly one already-made verdict into a real `Goal`/`Task` (or nothing, for the three verdicts that mean no new action). Never decides anything itself — deciding and acting are deliberately two different functions in two different files, mirroring how `Strategist` only returns decisions and `CEOBrain` is the one that applies them.
  - `src/atlas/brain/decisions.py` — `DecisionLog`, a separate JSON document (`.atlas/decisions.json`) holding every `Decision` ever made. Decisions are never mutated or deleted — a changed verdict for a category is a new `Decision` with `superseded_id` pointing at the one it replaced, so the full history of how ATLAS's judgment moved as evidence changed stays on record, inspectable via `atlas brain decisions show <category>`. Reuses `BrainStore`/`JSONFileStore`, same as `BrainMemory`/`KnowledgeBase`.
- `src/atlas/brain/cashflow.py` — pure, side-effect-free: `profit(goal, kpis)`/`roi(goal, kpis)`, computed on read from the existing `revenue_<goal_id>`/`cost_<goal_id>` KPI series — never a stored/fabricated KPI, and `None` (never `0.0`) when either side is unmeasured. `goal_cash_flow(goals, kpis)` is the shared "one entry per goal with revenue-or-cost measured" shape used by both `Reporter.summarize()` and `console.build_console_view()`, so the executive report and the live console can never drift apart on this number.
- `src/atlas/brain/valuation.py` / `scoring.py` — pure, side-effect-free support for the Strategist. `valuation.py` blends a `Goal`'s `founder_estimate` with its measured `revenue_<goal_id>`/`cost_<goal_id>` KPI history per criterion, weighted by `maturity()` (a saturating function of KPI reading count) — the founder's initial guess smoothly loses influence as real data accumulates, never a hard cutover; `scalability`, `automation_potential`, and `long_term_strategic_value` have no measured counterpart today and stay founder-judgment-only, by design, not oversight. `scoring.py` turns blended values into `score_cash_flow`/`score_strategic_value` per goal via min-max normalization *within that goal's own `horizon` bucket* — a short-term cash goal and a long-term asset-building goal are never ranked against each other.
- `src/atlas/brain/strategist.py` — `Strategist`/`SimpleStrategist`, the CEO's capital-allocation function: ranks active goals within their horizon bucket by the appropriate score and maps rank directly to `Goal.priority` (rank 1 = priority 1 = highest); pauses a goal only when it is bottom-ranked in its cohort *and* its own measured revenue shows no improvement over its last 3 readings (the same evidence-gating discipline `propose_improvements` uses for redesign candidates). Not a Registry asset — no manifest, no entrypoint, no capability protocol — a brain-internal component invoked from `CEOBrain.review()`, the same way `Planner`/`Prioritizer` are invoked from `tick()`. Returns decisions only; never mutates a `Goal` or touches `Registry`/`RiskPolicy` — `CEOBrain` applies and logs them itself.
- `src/atlas/brain/improvement.py` — `propose_improvements()`: the continuous-self-improvement principle, structurally enforced, not just documented. Only called from `review()` (never `tick()` — this alone limits churn), only emits a `redesign_*` candidate when it has evidence (a KPI flat/declined over its last 3 readings, or a task category's success rate below 50% over >=3 samples), and skips any target area with an open or recently-resolved (30-day cooldown) redesign task.
- `src/atlas/brain/ceo.py` — `CEOBrain`: `tick()` (plan → prioritize → risk-gate → delegate → monitor, then `absorb_opportunities()`, `_decide_and_apply()` — calls `decision_engine.decide_all()` fresh every cycle, persists+acts only on a materially-changed verdict via `decision_apply.apply_decision()` — and every pipeline-advance bridge above, in sequence — the frequent operational loop) and `review(period)` (evaluates previously `applied` proposals against their `baseline_metrics` → `confirmed`/`needs_review`; reallocates goal priority/status via the `Strategist` — auto-applied and logged, never risk-gated, since re-weighting an existing goal is not a structural change; generates improvement candidates; returns the executive report). `approve(task_id)`/`reject(task_id)` resolve either a plain risk-gated task or one with a linked `Proposal` through the same call; `approve()` also re-dispatches the task (so a founder's "yes" can drive e.g. Content Factory's next stage) and calls `record_revenue()` on the result, same as `tick()`.
- `src/atlas/brain/reporter.py` — `Reporter.summarize(period, memory, kpis)`: plain-dict report (active goals, tasks by status, pending approvals, blocked opportunities, open proposals, KPI deltas, reallocations, `cash_flow` via `cashflow.goal_cash_flow()`) for `daily`/`weekly`/`monthly` windows.
- `src/atlas/brain/console.py` — read-only aggregation over everything the brain already tracks, no new state: `build_console_view()` (goals/pending-approvals/blocked/every registered asset's `report()`/KPIs/`cash_flow` — iterates whatever is currently registered, so a future asset shows up automatically), `find_warnings()` (MAYA stopped / Revenue idle / pending redesign proposals / stale KPIs), `find_stale_kpis()`, `recent_activity()` (tail of the existing decision/outcome log), `build_briefing()` (narrative read of the same state), `get_queue()`/`get_opportunities()`/`get_campaigns()`/`get_system_health()`, `format_console_view()`. Shared by `atlas console`, the REPL's `status` command, and the full-screen app's dashboard — one implementation, not three.

`tests/fixtures/assets/` holds sample manifests used to test `atlas.core` and `atlas.brain` in isolation from the real `assets/` dir: `agent_sample` (`Runnable`), `business_sample` (metadata-only, no entrypoint), `triggerable_sample` (`Triggerable` + `Reportable`, for delegation/monitoring tests).

## Architecture: atlas.assets (operational agents)

Registered, real, tested assets today (see each `manifest.toml` for the authoritative `id`/`kind`/`categories`):

- **`maya`** — Asset #1. `MayaAgent.run()` currently only acknowledges delegated work; real content-generation capability is a stub. This is the single blocker for the whole Asset Engine (see `docs/ATLAS_BUSINESS_BLUEPRINT.md` §6) — nothing else in the Asset Engine can happen for real until this is built.
- **`research`** — discovers opportunities (schema: `{"description", "suggested_category", "source", "source_id"}`), consumed by `atlas.brain.intake.absorb_opportunities`. Produces no revenue signal itself, by design.
- **`revenue`** (`src/atlas/assets/revenue/agent.py`) — `RevenueAgent` routes a task by `category` to one of three placeholder channels under `revenue/channels/`: `AffiliateChannel` (`revenue_affiliate`), `DigitalProductChannel` (`revenue_digital_product`), `ContentAssetChannel` (`revenue_content_assets`). Each `execute()` currently always returns `revenue_generated: 0.0` — a real placeholder (honestly zero), not fabricated data — pending a real integration. **No channel exposes a cost signal today** — this is the standing, still-open gap `docs/ATLAS_BUSINESS_BLUEPRINT.md` §9 flags ("Investment (cost) — Live for Recruitment only"). Recruitment/workforce leads are explicitly *not* a channel here (see `recruitment_workforce` below) — that grew into its own standalone agent once its mission expanded past a single `execute()` call.
- **`recruitment_workforce`** — the only engine producing real, non-placeholder revenue/cost today. Full opportunity pipeline (`discovered → qualified → matched → proposal_ready → active → won/lost`), founder-approval gates (`approve_outreach`, `approve_commitment`), real bill-rate/pay-rate arithmetic. `Opportunity.goal_id`/`task_id` are set once at creation and never rewritten — the precise-attribution rule every later pipeline (affiliate, intelligence, content) copies.
- **`affiliate_department`** (Mission 003, `docs/FIRST_REVENUE_PIPELINE.md`) — `AffiliateDepartmentAgent`, category `affiliate_pipeline`. A reduced, 4-stage slice (`discovered → selected → content_planned`, plus `lost`) of the full 8-stage model `docs/AFFILIATE_OPPORTUNITY_MODEL.md` (Mission 002) designs. First `run()` creates 3 fixed placeholder candidates (no network access); scores them via `score_opportunity()` (`estimated_conversion × commission × (1-competition) × (1-content_difficulty)`, in `scoring.py`); drafts a content brief for the winner. Stops hard at founder approval — **no publishing, no external APIs, ever, in this asset**. A deliberately distinct manifest id from the pre-existing metadata-only `affiliate` (owned by `revenue`) to avoid silently repurposing that org-chart entry; dispatch category `affiliate_pipeline` is likewise distinct from `revenue_affiliate` so the two never collide at the `Delegator`.
- **`affiliate_intelligence`** — `AffiliateIntelligenceAgent`, category `affiliate_intelligence`. A *different* pipeline than `affiliate_department`: ranks candidate affiliate products (`discovered → researched → ranked`) via `DiscoveryAgent`/`ResearchAgent`/`RankingAgent` (`agents.py`), reusing `affiliate_department`'s `AffiliateOpportunity` model, `score_opportunity()`, and — critically — its **same store file** (`.atlas/affiliate_intelligence.json` is separate storage, but this and every asset below it share the *conceptual* opportunity records via `AffiliateStore`/`DEFAULT_STORE_PATH` imported from `affiliate_intelligence.agent`). Every opportunity reaches `ranked`; nothing is auto-selected. The founder chooses which to pursue via one approve/reject task per ranked opportunity.
- **`content_factory`** — `ContentFactoryAgent`, categories `content_factory` / `content_factory_editorial_fix`. Deterministic, template-based generation (`generator.py` — no LLM, no external API) of a full marketing content package (angles, hooks, headlines, CTAs, platform suggestions, video/image/carousel ideas) for an opportunity the founder already chose. Handles three distinct triggers: first generation, an Editorial Review fix request (regenerates only the failed sections), and a founder "request changes" rejection (regenerates fully, up to `REJECTION_LIMIT = 2` before permanently abandoning the opportunity). Planning only — never invokes MAYA's real (stubbed) capability, never publishes.
- **`editorial_review`** — `EditorialReviewAgent`, category `editorial_review`. Automatically evaluates every Content Factory package before the founder ever sees it, via 7 deterministic, rule-based checks (`checks.py`: quality, originality, brand consistency, spam risk, compliance/disclosure, marketing clarity, CTA quality — no LLM). `pass` / `revision_required` (≤2 failed checks) / `reject` (3+ failed, `MAX_REVISION_CYCLES = 2` exhausted). Each failed dimension maps to exactly one `content_package` section, so a fix request names precisely what to regenerate.
- **`publishing_gateway`** — `PublishingGatewayAgent`, category `publishing_gateway`. **The single controlled entry point between ATLAS and external platforms** — and still, deliberately, a dead end: builds and independently re-verifies (`builder.py`, re-running Editorial Review's own compliance check rather than trusting the upstream verdict) a `PublishPackage` from a founder-approved, editorial-passed opportunity, gates queuing behind one more founder approval, and stops at `QUEUED`. No external API is ever called; nothing is ever actually published. `PublishPackage` (`models.py`) is a distinct entity/state machine from `AffiliateOpportunity` — queue status is not conflated with content-approval status.

**The full affiliate chain** (`affiliate_department`/`affiliate_intelligence` → `content_factory` → `editorial_review` → `publishing_gateway`) is real, tested, and fully wired into `CEOBrain.tick()`, but produces zero real external revenue by design — every terminal state is either a founder-approval stop or `QUEUED` with no publish integration. See `docs/ATLAS_BUSINESS_BLUEPRINT.md` for the business-level Cash Engine / Asset Engine framing this all sits under, and note that document's own "Planned" list for Affiliate predates this chain and needs re-reading against the code, not trusted as-is.

`marketing`/`analytics`/`automation`/`cfo`/`coo` remain registered metadata-only, explicitly on hold per the standing instruction not to build them before there's real activity to justify them.

## Architecture: atlas.integrations (real platform connectivity)

A new, peer, dependency-free layer (2026-08-02) — not part of `atlas.core`/`atlas.brain`'s orchestration, importable by assets the same way stdlib is (`affiliate_department` imports it without breaking the "no atlas.core/atlas.brain imports" rule, since this isn't either of those). Exists specifically because platform integration is a real, separate concern from any one asset's business logic, and needs to be provider-agnostic from the start — never one integration per platform bolted onto whichever asset happens to need it first.

- `src/atlas/integrations/base.py` — two genuinely different Protocols, kept separate rather than one "PlatformProvider" interface (the same discipline `atlas.core.capabilities` already uses): `CommerceProvider` (`category`, `validate_link()`, `fetch_recent_sales()` — Digistore24, and future Amazon/AliExpress/Etsy/Shopify/Gumroad) for platforms with trackable links and sales data, and `ContentPublisher` (`publish()`) for platforms that publish content (YouTube/TikTok/Instagram) — defined, **zero implementations anywhere**, since `publishing_gateway` is still deliberately a dead end and building a real publisher is a separate, much bigger decision this layer's existence doesn't imply is ready. `CommerceProvider.category` is a structural fact (Digistore24 *is* an affiliate network) declared by the provider itself — requires no credential or network call, and is what lets `provider_ranking.rank_providers()` find every provider eligible for a category.
- `src/atlas/integrations/digistore24.py` — `Digistore24Provider` (`category = "affiliate"`), the first real `CommerceProvider`. `validate_link()` is fully real, live-verified against the actual production KetoDNA link, and is now the single source of truth for what a real Digistore24 link looks like (moved from `affiliate_department.models.validate_provider_link()`, which delegates here instead of duplicating the regex/parsing logic). `fetch_recent_sales()` is deliberately unimplemented — returns `None` with no `DIGISTORE24_API_KEY` configured, raises a clear `NotImplementedError` if one is set (Digistore24's exact current API surface hasn't been verified against real documentation/a real account, and shipping an unverified guess at code touching real financial data would be worse than leaving it honestly unbuilt). Real revenue recording today still goes through `atlas affiliate revenue record` (founder-reported, unchanged). The credential boundary is deliberate and general: **ranking/ selecting a provider never needs one; only operating a chosen provider for real ever does.**
- `src/atlas/integrations/registry.py` — `PROVIDERS`/`get_provider(name)`, the actual provider-agnostic mechanism: adding a platform means one new class satisfying `CommerceProvider` plus one registry entry, never touching an existing provider or any caller. `affiliate_department.models.SUPPORTED_PROVIDERS` was removed (2026-08-02) — it was a second, hand-maintained allowlist that had already drifted out of being derived from this registry; `get_provider()`'s own fail-closed check is the only place that question is answered now.
- `src/atlas/brain/provider_ranking.py` — `provider_confidence()`/`rank_providers()`: the mechanism that lets the Decision Engine choose *which platform* within an already-worth-pursuing category, not just whether the category is worth pursuing. Mirrors `confidence_score()`'s exact fail-closed shape one level deeper, built from `confidence.py`'s `source_corroboration_score()`/`recency_score()` generalized with an optional `provider` filter (default `None` preserves category-level behavior exactly) rather than duplicated at a new scope. Deliberately a narrower model than category-level confidence — `historical_success`/`measured_outcomes`/`internal_experiments` are absent entirely, since ATLAS doesn't yet attribute real revenue/cost per-provider, only per-goal. `decide()`'s `Decision.chosen_provider`/`context["provider_ranking"]` are populated from this on every `invest` verdict.

## Architecture: atlas.influencer (Digital Influencer Studio)

A new, peer layer (2026-08-03) — not part of `atlas.core`/`atlas.brain`'s orchestration, the same "genuinely separate concern" reasoning that justified `atlas.integrations` as its own package rather than living inside whichever asset needed it first. A `DigitalInfluencer` is a reusable business asset (a persona), not a Registry asset (not Task-dispatchable) and not a `Goal`/`Task` in the Brain's planning loop — it's a distinct entity the Decision Engine is meant to eventually *assign to* opportunities, so it gets its own bounded context.

- `src/atlas/influencer/models.py` — `DigitalInfluencer`, composed of five embedded sub-profiles (`IdentityProfile`, `VoiceProfile`, `VisualAvatarProfile`, `ContentStyleProfile`, `AudienceProfile`) plus `platform_targets: list[PlatformTarget]` and `asset_library: list[AssetLibraryEntry]`. Not tied to one platform or one avatar by construction: `platform_targets` is a list, and every sub-profile is generic across TikTok/YouTube/Instagram/future platforms alike. `categories: list[str]` is the explicit, founder-declared set of business categories (same open-string taxonomy `Finding.category`/`Task.category` use) this influencer can be assigned to — a structural fact declared by the entity itself, the same pattern `CommerceProvider.category` already established, never inferred from free-text niche/content_style. `VoiceProfile.provider`/`VisualAvatarProfile.provider` name a future synthesis/generation platform (ElevenLabs, HeyGen, ...) and stay `""` until one is actually credentialed — the same credential-boundary discipline `atlas.integrations` already established. `reference_sample`/`reference_image`/`AssetLibraryEntry.reference` are always real file paths/URLs, never fabricated/generated placeholders, mirroring `CreativeAgent.attach_real_asset()`'s exact discipline — no real image/voice/video generation integration exists yet. Nested sub-profiles need explicit `to_dict()`/`from_dict()` reconstruction (every other model in this codebase is flat and never hits this wrinkle — `dataclasses.asdict()` recurses out fine, but `Cls(**dict)` doesn't reconstruct nested dataclasses back in).
- `src/atlas/influencer/registry.py` — `InfluencerRegistry`: pure CRUD (`save_influencer`/`influencers`/`get_influencer`), the Digital Influencer Registry, same `BrainStore`/`JSONFileStore` atomic-write pattern as `KnowledgeBase`/`DecisionLog`/`Ledger`. `attach_asset()`/`add_platform_target()` are free functions (domain actions), not methods on the registry — same separation `kpi_intake.py`'s functions already draw from `KPIRegistry`'s pure storage.
- `src/atlas/influencer/performance.py` — Performance History, deliberately **not** a field on `DigitalInfluencer`: real measured data lives in `KPIRegistry` (`{metric_name}_{influencer_id}`, e.g. `followers_<id>`), the same separation `cashflow.py` already draws between a `Goal` and its measured revenue/cost. `record_metric()` is fully generic — any metric name, since TikTok's vocabulary isn't YouTube's — and a **replacement** reading (a follower count is a fact about right now, not an amount to sum). `performance_snapshot()` reads back `STANDARD_METRICS` (`followers`/`views`/`engagement_rate`) by default, `None` per metric never recorded, never fabricated.
- `src/atlas/influencer/ranking.py` — `rank_influencers(category, registry, kpis)`: the Learning & Optimization foundation. Filters to active influencers explicitly tagged for `category`, ranks by `factors_available` (how much real performance evidence is on record) — **deliberately not** a blended numeric confidence score like `confidence_score()`/`provider_confidence()`: raw metrics (followers, views, engagement_rate) live on incompatible scales, and inventing a normalization/weighting scheme before real data exists to justify one would be the fabricated-precision mistake avoided everywhere else in this codebase. Same bootstrapping shape `provider_ranking.py` started from (zero factors) before its first real Finding — a real weighted `influencer_confidence()` is future work once enough real performance history exists to justify how metrics should be weighted against each other.
- **Deliberately not built yet**: platform-specific publishing (no `ContentPublisher` implementation exists for any platform — same standing gap `atlas.integrations` already has), and Decision Engine wiring (`Decision.chosen_influencer`, `Goal.influencer_id`) — the founder's request was explicitly the reusable foundation first; wiring `decide()` to actually assign an influencer touches `Decision`/`Goal`/`decision_apply.py`, all locked components, and would be premature before a single real `DigitalInfluencer` has any real performance history to rank on. Flagged as the natural next increment, not bundled into this one.
- CLI: `atlas influencer create/list/show`, `atlas influencer asset attach`, `atlas influencer platform add` (a declared target, never a publish action), `atlas influencer metric record`, `atlas influencer rank <category>`.

### Content Production Layer (`atlas.influencer.production`, 2026-08-03, architecture locked; rewired 2026-08-03 to generate from Campaign)

Sits above the Digital Influencer Registry. **Superseded its original design the same day it was built**: `ProductAssignment` (a per-influencer work-assignment record) and `atlas influencer product assign`/`atlas influencer produce <influencer_id> <product_assignment_id>` were removed entirely once `atlas.campaign` introduced `Campaign` as the real unit of work — "The Production Layer should generate all required assets from the Campaign, not from isolated product assignments" (founder's framing, verbatim intent). Keeping both would have been exactly the duplicated-relationship problem this codebase has repeatedly eliminated elsewhere (e.g. `SUPPORTED_PROVIDERS`'s removal) — `Campaign.product_offer` + `Campaign.influencer_ids` is now the one place "which influencer is producing content for which product" is recorded.

- `TEMPLATE_KINDS` (models.py) — one open-but-bounded, documented set (`script_template`, `hook`, `cta`, `image_prompt`, `video_prompt`, `voice_prompt`, `caption_template`, `landing_page_message`) backing a single `ContentTemplate` dataclass, deliberately not eight near-identical dataclasses that would all share the exact same real shape (name, founder-authored content, tags). A new library type is a new string in this set, never a new field/dataclass/CLI command — the same "explicit, documented, editable set" discipline `confidence.CATEGORY_TASK_CATEGORIES` already established. `production.add_template()`/`templates_of_kind()` are unchanged by the Campaign rewire — a template still belongs to the influencer that owns it, regardless of which campaign later uses it.
- `production.generate_content_package(campaign_id, influencer_id, campaign_registry, influencer_registry)` → `ContentPackage` (models.py, `campaign_id` + `influencer_id`, no more `product_assignment_id`) — assembles one influencer's templates for one campaign, substituting the literal `{product_name}` placeholder with `campaign.product_offer` (no other placeholder syntax is interpreted). **No real generation happens** — deterministic template assembly only, the exact same "no LLM, no external API, no image/video/voice generation" boundary `content_factory/generator.py` already established. `missing_kinds` names every `TEMPLATE_KINDS` entry this influencer has no template for — an honest, visible gap. `production.generate_campaign_content(campaign_id, ...)` composes this once per `campaign.influencer_ids` entry for a multi-influencer campaign — a thin loop, not a second assembly mechanism. `ContentPackage` stays deliberately **not persisted** — purely computed on demand, the same read-only-view shape `explain_opportunity()` already has.
- CLI: `atlas influencer template add/list` (unchanged); `atlas campaign produce <campaign_id>` (replaces the removed `influencer produce`).

## Architecture: atlas.campaign (Campaign Intelligence Layer)

A new, peer layer (2026-08-03, architecture locked) sitting between the Decision Engine and the Content Production Layer in the full intended pipeline: **Research → Evidence → Decision Engine → Campaign Intelligence → Digital Influencer → Production → Publishing → Measurement → Finance → Learning**. `Campaign` is "the business unit that ATLAS manages end-to-end" (founder's framing) — it replaces an isolated product assignment as the real unit of work the Production Layer generates from.

- `src/atlas/campaign/models.py` — `Campaign`: `business_objective`, `revenue_goal`, `target_audience`, `customer_problem`, `product_offer`, `influencer_ids` (plural — a campaign can involve multiple influencers), `platform_strategy`, `content_strategy`, `content_formats`, `landing_page_strategy`, `cta_strategy`, `budget`, `timeline`, `success_kpis`, `learning_history`, `confidence_score`, plus `category` (structurally necessary — the same justified, non-fabricated addition `DigitalInfluencer.categories` already was, since it's what ties a Campaign back to `confidence_score()`'s evidence taxonomy) and `goal_id` (the real Goal this campaign executes under, when one exists). Flat dataclass — no nested sub-profiles, unlike `DigitalInfluencer`, so `Campaign(**data)` reconstructs it directly. `revenue_goal`/`budget`/`timeline` are stated targets/plans, never measured facts — the same class of transparent assumption `ASSUMED_MONTHLY_LEADS` already is; real measured performance stays in `KPIRegistry`/`Ledger` via `goal_id`, never duplicated onto the Campaign itself.
- `src/atlas/campaign/registry.py` — `CampaignRegistry`: pure CRUD, same `BrainStore`/`JSONFileStore` pattern as every other registry in this codebase. `create_campaign(...)` is the Campaign Intelligence Layer's real output — fail-closed (every named influencer must already exist in `InfluencerRegistry`), and `confidence_score` reuses the exact category-level `confidence.confidence_score()` the Decision Engine's own `decide()` already computes from (Intelligence Layer, unchanged) — never a new, parallel scoring mechanism invented just for campaigns. `refresh_confidence(campaign_id, ...)` recomputes it from current evidence and appends a `learning_history` entry recording the change (never silently overwriting the prior value) — the campaign-level expression of "nothing is permanently true," adapted to a mutable, in-place-updated entity instead of `Decision`'s immutable, append-only-with-`superseded_id` shape.
- **Deliberately not built yet**: the Decision Engine does not call `create_campaign()` automatically. "The Decision Engine should... create a complete Campaign object" (founder's stated intent) describes the target contract; wiring `decide()`/`decision_apply.py` to build one on every `invest` verdict is a separate, later increment — those are locked components, and there is still no real campaign performance history to justify an automatic choice of which influencer(s)/strategy to assign. `create_campaign()` is a direct, explicit call today (CLI or programmatic), ready to be the exact function that wiring would call.
- `link_goal(campaign_id, goal_id, registry)` / `set_status(campaign_id, status, registry)` (2026-08-03) — `link_goal` attaches a real Goal to a campaign created without one (`create_campaign()` only accepts `goal_id` at creation); `set_status` is the founder's explicit approval gate — moving a campaign to `"active"` is what "receiving an approved Campaign" means today (see Execution Orchestrator below), fail-closed against `CAMPAIGN_STATUSES` (`proposed`/`active`/`paused`/`completed`/`cancelled`).
- **`atlas campaign revenue/cost/fee/settlement/refund record`** (2026-08-03) — a real, previously-missing gap, found by verification rather than requested: the existing `atlas affiliate revenue/cost/fee/settlement/refund record` commands all resolve `package_id -> goal_id` via `PublishingQueueStore` (`_resolve_package_goal_id`), the old `affiliate_department`/`publishing_gateway` chain. A `Campaign` never creates a `PublishPackage`, so there was no real way to record real money against a Campaign's `goal_id` at all — the entire Measurement/Finance/Ledger layer was unreachable for the whole Campaign/Orchestrator pipeline, and `check_measurement` could never leave `"blocked"` for any Campaign-driven goal. Fixed with a second, `campaign_id`-based resolver (`_resolve_campaign_goal_id`, mirrors `_resolve_package_goal_id`'s fail-closed shape) calling the exact same, unmodified `record_manual_revenue()`/`record_manual_cost()`/`record_manual_settlement()`/`record_manual_refund()` functions — `goal_id` was always the real parameter; `package_id` resolution was only ever a CLI convenience for the old flow. No changes to `kpi_intake.py` were needed.
- CLI: `atlas campaign create/list/show/refresh-confidence/produce/activate/link-goal/revenue/cost/fee/settlement/refund`.

## Architecture: atlas.orchestrator (Execution Orchestrator)

A new, peer layer (2026-08-03, architecture locked) sitting between the Campaign Intelligence Layer and real business execution — "the operating layer that turns a Campaign into real business execution" (founder's framing). **Coordinates; never executes.** Every real action it can trigger routes through machinery this codebase already has: `produce_content` calls the existing Content Production Layer directly (ATLAS's own deterministic assembly, still not "the work"), and `request_founder_review` only ever creates a plain `Task` — `RiskPolicy`/`Delegator`/`CEOBrain`'s own existing `tick()` loop is what actually decides and acts on it, on its own next cycle, exactly "as a CEO coordinates departments" rather than doing the work personally.

- `src/atlas/orchestrator/models.py` — `ExecutionStep` (`kind` — `verify_readiness` | `produce_content` | `request_founder_review` | `check_measurement`, an explicit documented set mirroring `influencer.models.TEMPLATE_KINDS`; `depends_on` — other step ids in the same plan; `status` — `pending`/`dispatched`/`done`/`blocked`/`failed`; `task_id` correlates a dispatched step to the real `Task` it created, the same correlation-key pattern `source_opportunity_id`/`engine_id` already establish; `result` — a structured, honest record, never a bare pass/fail flag) and `ExecutionPlan` (embedded `steps`, `event_log` — the plan's audit trail, the same provenance discipline `Campaign.learning_history` already established one layer up). "Event-driven" here means what it means everywhere else in this codebase: `advance_execution()` recomputes fresh from current real state every call (the same purity `decide()` already relies on), not a message-bus architecture — none exists anywhere in this codebase, and none is built here.
- `src/atlas/orchestrator/registry.py` — `ExecutionPlanRegistry`: pure CRUD, same `BrainStore` pattern as every other registry.
- `src/atlas/orchestrator/orchestrator.py`:
  - `start_execution(campaign_id, ...)` — builds the DAG: one `verify_readiness`, one `produce_content` per `campaign.influencer_ids` entry (all depending on `verify_readiness`), one `request_founder_review` depending on every `produce_content` step, one `check_measurement` depending on that. Requires `campaign.status == "active"` — refuses to start execution on anything not yet founder-approved. Idempotent (returns the existing `in_progress` plan rather than starting a second one).
  - `advance_execution(plan_id, ...)` — the core loop: re-evaluates every step's readiness fresh each call and acts on whatever just became ready, in the same pass (a step's dependency completing earlier in the same iteration unblocks it immediately — this **cascades multiple steps within one call**, not "one step per call"). This single mechanism is both "track execution state and failures" and "resume automatically when blocked tasks become available" — a step blocked because its precondition wasn't met yet (missing Goal, missing templates, unmeasured revenue) is simply re-evaluated fresh the next time this runs, no separate retry mechanism needed. Closes the loop into Measurement/Finance/Learning via `check_measurement`: once real `profit()` exists for `campaign.goal_id`, it calls `campaign.refresh_confidence()` (unchanged, reused) and marks itself done. When every step in a plan is `done`, the plan and its Campaign both move to `"completed"` — "close the Campaign only when all execution stages are completed," literally.
  - `advance_all_campaign_executions(...)` — the `CEOBrain.tick()` bridge, the same shape every pipeline-advance bridge already has (`advance_affiliate_pipeline`, `advance_content_factory`, ...): read current state, advance whatever's ready, persist. Wired into `tick()` after every other bridge; `CEOBrain` gained `campaigns`/`influencers`/`execution_plans` constructor params (same optional-with-default pattern as `ledger`) to support it.
- The bridge from the existing opportunity-driven chain (`affiliate_department`/`content_factory`/`editorial_review`/`creative_agent`/`publishing_gateway`, which operates on `AffiliateOpportunity`, not `Campaign`) is now partially built — see **atlas.brain.campaign_advance** below — but only for the one real signal that exists today (a founder-selected affiliate product); `request_founder_review` remains the orchestrator's only real hand-off to a "specialized agent," since there is still no real `ContentPublisher`.
- Live-verified the complete lifecycle in an isolated scratch dir end-to-end through real `CEOBrain.tick()`/`brain approve` calls (not direct orchestrator calls): plan created → `verify_readiness`/`produce_content`/`request_founder_review` cascaded in one `advance` call → real Task reached `pending_approval` → founder approved it via `atlas brain approve` → the *same* next `tick()` picked up both the approval and re-advanced the plan → real revenue/cost recorded → final `tick()` completed `check_measurement`, closed the plan, and closed the Campaign, with real profit ($100) and a full `event_log` audit trail.
- CLI: `atlas campaign execution start/advance/show`.

## Publish-readiness (2026-08-03) — "ready" means publishable, not just media-ready

The founder's own bar, stated directly: "I want 'ready' to mean that a campaign has everything required for publication" — real media, title, description, captions, hashtags (when applicable), landing page/destination URL, product/offer assignment, CTA, and platform-specific requirements. `orchestrator._produce_content()`'s "done" criteria *is* this bar now, not a separate "publish-ready" state — the existing done/blocked step semantics were reused, not forked.

- `influencer.models.TEMPLATE_KINDS` gained `"title"`, `"description"`, `"hashtags"` — genuinely distinct from `"hook"`/`"caption_template"` for platforms that separate them (a YouTube title vs. its description), the same open-but-bounded-set extensibility `TEMPLATE_KINDS` was already designed for ("a new library type is a new string in this set, never a new field/dataclass"). `ContentPackage`/`generate_content_package()` gained matching `titles`/`descriptions`/`hashtags` fields.
- `orchestrator.PUBLISH_ESSENTIAL_TEMPLATE_KINDS = {"title", "description", "hook", "cta", "caption_template"}` supersedes the old, narrower `_ESSENTIAL_TEMPLATE_KINDS = {"hook", "cta"}`. `HASHTAG_PLATFORMS = {"TikTok", "Instagram", "Twitter", "X"}` is a stated, editable assumption (the same class as `affiliate_pipeline_advance.ASSUMED_MONTHLY_LEADS`) — hashtags are only required when the influencer targets one of these, honoring the founder's own "(when applicable)" qualifier literally rather than requiring them unconditionally. `video_prompt`/`image_prompt`/`voice_prompt` stay optional — generation aids superseded by real attached media, not requirements once real media exists.
- **`Campaign` gained `destination_url: str = ""`** (`atlas/campaign/models.py`) — the single most significant finding of this review: `campaign_advance.py` was copying `opportunity.product_name` into `Campaign.product_offer` but never copying `opportunity.real_affiliate_link`. A campaign could reach founder approval with a CTA that says "click the link" and no real, verified link anywhere on it. Now copied automatically from the real, already-validated `AffiliateOpportunity.real_affiliate_link` when the Decision Engine bridge creates a campaign; `campaign.registry.link_destination_url()` (mirrors `link_goal()`) attaches one after the fact for campaigns created without one (e.g. via CLI).
- `_produce_content()` now also requires: at least one real `AssetLibraryEntry` (`atlas influencer asset attach`, already built, previously unused as a gate), at least one declared `PlatformTarget` (`atlas influencer platform add`, same story), a non-empty `campaign.destination_url`, and a non-empty `campaign.product_offer`. Every missing requirement is named explicitly in the step's `blocked` reason — never a bare "not ready" with no explanation.
- CLI: `atlas campaign create --destination-url`, `atlas campaign link-destination-url`.
- Live-verified: a campaign built to only the *old* bar (hook+cta) now correctly blocks with an itemized list of everything still missing; adding title/description/caption/real media/platform declaration resumes it automatically on the next `advance` call, no special retry; a TikTok-targeting influencer correctly blocks on missing hashtags while an otherwise-identical YouTube-targeting one does not, confirming the "(when applicable)" conditionality actually works.

## Architecture: atlas.assets.campaign_execution (the real request_founder_review handler)

**Standing operating principle (2026-08-03), governs all future ATLAS work**: ATLAS evolves as a Business Operating System, not a feature collection. Before building anything, the question is "does this move ATLAS closer to autonomously operating a real business?" Always prioritize whatever removes the largest remaining blocker to autonomous end-to-end execution over net-new capabilities. Role framing to keep consistent: Decision Engine = the CEO, Campaigns = the business units, Digital Influencers = business assets, Production = creates the assets, Execution (Orchestrator) = performs the work, Finance = measures reality, Learning = improves future decisions.

This asset is the first concrete product of that principle, chosen via a verified gap analysis (not proposed from scratch): approving a Campaign's `request_founder_review` `Task` previously fell through `Delegator.delegate()`'s unmatched-category fallback (`src/atlas/brain/delegator.py`) to an arbitrary Triggerable asset — confirmed by direct code trace, not assumption — since no asset declared category `"campaign_execution"`. Approval led to a semantically meaningless dispatch, not to anything representing what actually needs to happen next; this was the single largest verified gap between the current pipeline and something that behaves like a real business.

- `src/atlas/assets/campaign_execution/` — a real, minimal Registry asset (manifest.toml + `CampaignExecutionAgent`), the same self-contained pattern every other department already uses. No special-case bypass was added to `Delegator`/`RiskPolicy`/`CEOBrain` — registering a real asset for the category is what makes `Delegator`'s own `matched` lookup find it, exactly the mechanism every other real department already relies on. `run(task)` looks up the real `Campaign` for `task.goal_id` (via `CampaignRegistry`, the same peer-layer import precedent `atlas.integrations` already established) and returns an honest, concrete next step — "no real publishing integration exists yet, post it yourself, then record real results via `atlas campaign revenue/settlement record`" — never fabricating a "published" event. `report()` is necessarily aggregate (`Reportable.report()` takes no arguments, the same shape every asset's `report()` already has) — computed fresh from `CampaignRegistry` every call, never cached in-memory state (an asset instance doesn't survive across separate CLI/tick invocations; only real, durable storage does).
- **Test-isolation gap found and fixed while verifying this** (same class caught twice before, for `Ledger` and `AffiliateStore`): `CEOBrain`-based test helpers' `CampaignRegistry`/`InfluencerRegistry`/`ExecutionPlanRegistry` paths didn't include the `.atlas/` prefix their real cwd-relative defaults use — invisible until this was the first asset to actually reference `CampaignRegistry` from inside a Registry-instantiated agent. Fixed across `test_ceo.py`/`test_app.py`/`test_repl.py`.
- Live-verified end-to-end: `assigned_asset_id` is now `"campaign_execution"`, not an arbitrary asset; the full lifecycle (evidence → invest → real product selection → Campaign → cascade → approval → this agent → real revenue recorded → campaign closes) still completes correctly with the fix in place.

## Architecture: atlas.brain.campaign_advance (Decision Engine → Campaign bridge)

Connects the Decision Engine's category-level `"invest"` verdict to a real, running Campaign end-to-end (2026-08-03, architecture locked): "the Decision Engine should... choose the highest-confidence opportunity, choose the most suitable Digital Influencer, create the Campaign, assign the selected product, generate the complete content package, advance the workflow automatically through every internal ATLAS stage. Stop exactly at the first external boundary" (founder's framing, verbatim intent). Invents no new evidence-scoring anywhere in this bridge — every real decision it acts on was already made by an existing, unmodified mechanism.

- **Real architectural finding, surfaced before building** (see conversation, not restated in full here): the Decision Engine's `decide()` operates at the *category* level and has no concept of a specific product. The one real, non-fabricated "founder chose this specific real product" signal is `AffiliateOpportunity.stage == "selected_for_marketing"`, reached only through the existing, untouched `atlas affiliate product add` → `affiliate_intelligence_advance.py` founder-choice `Task` → `approve()` flow. That exact signal was already claimed by a separate, complete, already-automated pipeline (`content_factory_advance.py` → `editorial_review_advance.py` → `creative_agent_advance.py` → `publishing_gateway_advance.py`) — bridging Campaign creation to the same signal without coordinating would let two independent pipelines both generate content and both request founder approval for the same real opportunity.
- `src/atlas/brain/campaign_advance.py` — `advance_decision_driven_campaigns(...)`: for every **active** `Goal` whose `engine_id` matches `"intelligence_{category}"` (`decision_apply.py`'s existing correlation key, unmodified) where `category` is in `BRIDGED_CATEGORIES` (`{"affiliate"}` today — the only category with a real per-product selection mechanism; `digital_product`/`content` have none, and none is invented here), with no Campaign yet for that goal: looks up the real selected opportunity (`AffiliateStore`, `stage == "selected_for_marketing"`, exact match — a stage the old pipeline has already advanced past self-resolves out of this query, no explicit lock needed) and the top-ranked real influencer (`rank_influencers()`, unchanged, previously built but unwired) for that category. If both exist, calls `create_campaign()` (unchanged) with the real product name and influencer, immediately `set_status(..., "active", ...)` (an internal, reversible, zero-cost workflow transition — the same class of directly-applied action `Strategist`'s own goal reallocation already is, never routed through `RiskPolicy`, since nothing real-world happens yet), and `start_execution()` (unchanged). If no product or no influencer is available yet, does nothing — re-evaluated fresh next tick, the same resumability every other mechanism here has.
- **Race avoidance with the old pipeline** (real, not speculative — found via the architectural review above): `content_factory_advance.advance_content_factory()` gained an optional `campaign_claimed_goal_ids` parameter (defaults to `set()`, so every existing caller/test is unaffected) — a goal already claimed by a real Campaign is excluded from `_trigger_generation`'s (and therefore the whole function's) known-goal set. `CEOBrain.tick()` calls `advance_decision_driven_campaigns()` **before** `advance_content_factory()` so a goal claimed this same tick is already reflected by the time the guard checks — this ordering is what makes the exclusion race-free within one tick, not just eventually consistent. `CEOBrain` gained an `affiliate_store` constructor param (same optional-with-default pattern as `ledger`) pointed at `affiliate_intelligence.agent.DEFAULT_STORE_PATH` (`.atlas/affiliate_intelligence.json`) — the shared store file Affiliate Intelligence/Content Factory/Editorial Review/Creative Agent/Publishing Gateway already all use and the only place `selected_for_marketing` is ever set; `affiliate_department.json` is a different file for the separate, placeholder-only discovery chain that never reaches that stage.
- Live-verified twice in isolated scratch dirs, replaying the real founder workflow (`brain finding add` → `brain tick` → `affiliate product add` → `brain tick` → `brain approve` the founder-choice task → `brain tick`), not just direct function calls: (1) with the influencer registered *before* product approval — the new bridge won cleanly, created and activated a real Campaign, cascaded through every internal stage to `request_founder_review`, and zero `content_factory` tasks were created; approving the review task and recording real revenue via `atlas campaign revenue record` then closed the plan and the Campaign automatically, with confidence rising from 0.778 to 0.930 as real profit joined the evidence. (2) With the influencer registered *after* an old-pipeline task was already in flight for the same opportunity — the old pipeline's task (created and risk-gated on an earlier tick, `reversible=True`, auto-delegating) completed and advanced the opportunity's stage to `content_packaged` within the same tick, *before* the new bridge's turn — so the bridge correctly found nothing to claim and created no Campaign, with no double-processing. Both directions of the race resolve safely, one by the explicit guard, one by the pre-existing stage-progression property.

## Architecture: Opportunity Discovery V1 (2026-08-03) — evidence-driven, no hardcoded products

The founder's requirement, verbatim intent: ATLAS must discover, rank, and explain real opportunities itself — no hardcoded products, no manual product/niche selection — then feed the winner into the existing Campaign pipeline. Development-only per explicit instruction: **every new capability here stays disconnected from real production until explicitly approved**, even though development itself continues (the pilot's real-world execution is a separate, still-open concern). See the plan at the time this was built for the full gap analysis; summarized below is what's real in code now.

- **Two verified, pre-existing gaps this closes**: (1) the Decision Engine's own autonomous `"invest"` verdict for `"affiliate"` bootstrapped into `AffiliateDepartmentAgent` (`affiliate_pipeline`) — a documented dead end whose `discovered → selected → content_planned` chain never reaches `selected_for_marketing` and therefore can never reach `campaign_advance.py`'s bridge, no matter how much evidence accumulates. The only path that ever reaches Campaign is `affiliate_intelligence`, previously populated exclusively by founder-manual `intake_real_product()`. (2) Two incompatible opportunity-scoring mechanisms existed: `confidence_score()` (real evidence) vs. `score_opportunity()` (founder-judgment placeholders) — no evidence-based ranking existed for one specific product/topic.
- **`atlas/brain/feature_flags.py`** — `opportunity_discovery_v1_enabled()`, gated on `ATLAS_OPPORTUNITY_DISCOVERY_V1` (unset by default), the literal Dev/Production separation switch. With it unset, every new mechanism below is a documented no-op — the real, currently-running Windows Scheduled Task tick is completely unaffected by this code existing in the working tree. Same env-var-gated-inertness precedent as `DIGISTORE24_API_KEY`.
- **`Finding` gained `subject`/`market`** (`atlas/brain/models.py`) — two more optional, orthogonal scoping dimensions, the same shape `provider` already established. `subject` names the specific candidate product/topic evidence is about; `market` names the country/language when known. Neither inferred — both `""` unless the source states them.
- **`atlas/brain/opportunity_ranking.py`** — `rank_opportunities()`/`opportunity_confidence()`: the same "evidence → `weighted_average_of_available()` → rank" pattern already applied at category level (`confidence.py`) and provider level (`provider_ranking.py`), applied a third time, one level deeper, to `(category, subject)`. `recommended_market` is the most common real `Finding.market` behind a subject's evidence, never a guess. `source_corroboration_score()`/`recency_score()` in `confidence.py` gained an optional `subject` param (mirrors `provider`) rather than being duplicated.
- **`atlas/integrations/base.py` gained `MarketSignalProvider`** (Protocol) and **`atlas/integrations/signal_registry.py`** (`SIGNAL_PROVIDERS`, starts empty) — mirrors `CommerceProvider`/`ContentPublisher` exactly. Reserved: zero implementations exist. Picking a real, credentialed demand-signal source (search trends, a marketplace catalog, social trending topics) is a separate, later decision — this V1 builds the pipe, not the tap.
- **`confidence.OPPORTUNITY_DISCOVERY_BOOTSTRAP_OVERRIDES`** (`{"affiliate": "affiliate_intelligence"}`) + `decision_apply.py` consulting it only when the flag is on — the fix for gap (1) above, fully reversible by unsetting one env var.
- **`atlas/brain/opportunity_discovery_advance.py`** — `advance_opportunity_discovery()`: for every active, Decision-Engine-created goal in a bridged category, ranks real evidence and creates a bare `AffiliateOpportunity` at stage `"ranked"` for any subject clearing `MIN_INDEPENDENT_SOURCES` not already surfaced (idempotent). Lands at the exact same stage `intake_real_product()` uses, so the existing, unmodified `affiliate_intelligence_advance._request_founder_choice()` picks it up for free — no changes needed there. **Never fabricates commercial terms**: `commission_per_conversion`/`real_affiliate_link` stay `0.0`/`""` — evidence picks *which niche*, never *what deal was signed*; the founder still supplies real terms, same as today. Wired into `CEOBrain.tick()` right before `advance_affiliate_intelligence()`.
- CLI: `atlas brain finding add --subject --market`; `atlas brain opportunities --explain` additionally lists ranked specific opportunities per category.
- **Live-verified end-to-end in isolated scratch dirs** (never the real `.atlas/` production data): with the flag unset, seeding real findings and ticking reproduces exactly today's production behavior (bootstraps `affiliate_pipeline`, the dead end) — zero change. With the flag set, the same evidence instead bootstraps `affiliate_intelligence`, `advance_opportunity_discovery` creates a real ranked `AffiliateOpportunity` from evidence alone (confirmed zero when evidence is below the 2-independent-source bar), the existing founder-choice task appears in the same tick, and approving it plus registering a real influencer lets the existing, unmodified `campaign_advance.py` bridge create and activate a real Campaign — confirmed the resulting campaign correctly stays blocked on publish-readiness (no destination URL, no content templates) until the founder supplies real commercial terms, exactly the intended boundary.

**Second increment (same day)** — market/influencer recommendation, built and tested in isolation before integrating, one piece at a time:
- **`AffiliateOpportunity.recommended_market`** (`affiliate_department/models.py`) — the real field the notes-only version from the first increment was missing; now set structurally by `opportunity_discovery_advance.py` from `opportunity_ranking`'s evidence, not just embedded in a free-text note.
- **`opportunity_ranking.explain_opportunity_subject()`** — the opportunity-level "why is this promising" (evidence citations, recommended market, honest risks — deliberately omits ROI/probability-of-success, since neither is attributed per-subject, only per-goal). Wired into `atlas brain opportunities --explain`.
- **`influencer.ranking.prefer_market_match()`** — pure function, unit-tested standalone before touching `campaign_advance.py`: prefers a real `IdentityProfile.language` match for `opportunity.recommended_market` among `rank_influencers()`'s already-ranked candidates, exact match only, never fuzzy. Falls back to `ranked[0]` — today's exact behavior — whenever `recommended_market` is `""` (true for every founder-manual intake), proven by an explicit regression test, not just by reading the code. Live-verified: an influencer with weaker raw evidence but the right market beat a higher-ranked influencer in the wrong market.
- **Founder's decision (2026-08-03) on the "no matching influencer at all" gap**: reuse the existing `create_asset` structural-proposal path — never auto-generate an identity. `campaign_advance._missing_market_influencer_task()` (built and unit-tested in isolation before integration) creates a real, `pending_approval` Task/Proposal, correlated to the specific opportunity via `source_opportunity_id` (never repeated once raised). **Non-blocking**, matching how `propose_capability` never blocks the rest of the system: the campaign is still created immediately with the best real influencer available; the proposal exists in parallel so the founder can later decide whether to create a market-specific identity. Live-verified: `atlas brain proposals` shows a real, human-readable `create_asset` proposal naming the missing market and which influencer was used instead, while `campaign list` shows the campaign already active.

## Architecture: Digital Influencer Factory (2026-08-03)

The founder's explicit next priority: "ATLAS can automatically propose a new influencer for the recommended country, language, market, and niche, waiting for Founder approval before creation." Deliberate, permanent scope line (not a placeholder for future generation): ATLAS proposes **WHERE/WHAT** — a new module, `src/atlas/influencer/factory.py`, built and tested in three isolated pieces before any wiring:

- **`InfluencerDraft`** — market/niche/category/rationale/evidence, every field either a direct evidence citation or a plain structural fact already on the real `AffiliateOpportunity`. The dataclass itself has no name/personality/bio/voice/visual field — "no fabricated identity" is a structural guarantee, not just a convention (a dedicated test asserts this against `dataclasses.fields()`, not just the docstring).
- **`draft_influencer_proposal(opportunity, knowledge)`** — reuses `opportunity_ranking.explain_opportunity_subject()` entirely rather than recomputing anything; honestly empty evidence/rationale for founder-manual intake (no subject-tagged Finding trail to cite).
- **`create_influencer_from_proposal(task_id, name, ...)`** — materializes the real `DigitalInfluencer`, founder supplies WHO (name required; personality/bio optional). **Fail-closed on approval ordering, live-verified**: raises `ValueError` if the task isn't a real Factory proposal, and raises `ValueError` (`"has not been approved yet"`) if `task.status != "done"` — creation is structurally impossible before `CEOBrain.approve()` resolves the linked Proposal, not just documented as expected order. Recomputes the draft fresh from the real opportunity rather than trusting a stored copy, so it can never go stale.
- **`campaign_advance._missing_market_influencer_task()`** now embeds the real draft (market/niche/category/rationale/cited evidence URLs) directly in the proposal's description, instead of a bare capability-gap sentence.
- CLI: `atlas influencer create-from-proposal <task_id> --name ... [--personality] [--bio]`.
- Live-verified end-to-end in a scratch dir: real evidence → Factory proposal raised (non-blocking, campaign proceeds with the fallback influencer) → `create-from-proposal` correctly refused pre-approval → `brain approve` → `create-from-proposal` succeeds, producing a real `DigitalInfluencer` with market/niche/category from evidence and identity from the founder.

**Extended (2026-08-03, same day)** — the founder asked for nationality, native language, local name, personality, age range, communication style, visual style, target audience, and preferred platforms. Split deliberately in two, per the same "never fabricate" boundary the Factory was built on:
- **Built now — real, fact-grounded**: `IdentityProfile` gained `nationality: str` (new, real field — the one narrow exception to "every IdentityProfile field is founder-authored," since it's a plain world-fact lookup, not an invented identity detail). `InfluencerDraft` gained `nationality`/`native_language` (via `MARKET_LOCALE`, a transparent, editable market→locale table — same "stated, editable assumption" class as `HASHTAG_PLATFORMS`, never guessed for an unlisted market) and `recommended_audience` (a template built purely from real niche+market, not fabricated demographics). `prefer_market_match()` and `_missing_market_influencer_task()` now compare `identity.nationality` (not `language`, which was being overloaded with market codes before this) — `language` now correctly holds the actual spoken language. CLI: `influencer create --nationality`.
- **Founder's decision on the remaining fields (2026-08-03, same day)**: generate one specific, clearly-labeled AI-suggested value per field (not a blank brief) — `local_name`, `personality`, `age_range`, `communication_style`, `visual_style`, `preferred_platforms`. Built as `PersonaSuggestion`/`suggest_persona()`, a structurally **separate type** from `InfluencerDraft` — a real recommendation and a creative suggestion can never be confused for each other by anything that consumes either.
  - Deterministic, not generative: no LLM/image-generation integration exists anywhere in this codebase, so "AI-suggested" means a curated, transparent, hash-seeded selection from real pools (`NAME_POOLS`, `AGE_RANGES`, `COMMUNICATION_STYLES`, `VISUAL_STYLES`, `PLATFORM_SUGGESTIONS`) — the same "stated, editable assumption" class as `MARKET_LOCALE`, not a live generation call. `suggest_persona()` is seeded off `source_opportunity_id`, so the same proposal always yields the same suggestion — approving it and creating from it later never surprises the founder with a different draft than the one they reviewed. `NAME_POOLS` is nationality-aware (falls back to a neutral default pool for an unlisted nationality, never a culturally wrong guess).
  - `IdentityProfile` gained `age_range`. `communication_style`/`visual_style` map onto the existing `ContentStyleProfile.tone`/`VisualAvatarProfile.description` fields — no new sub-profile needed.
  - `create_influencer_from_proposal()`'s WHO parameters (`name`/`personality`/`age_range`/`communication_style`/`visual_style`) are all `None`-by-default now, meaning "use the suggestion" — passing an explicit value overrides just that one field, the rest still default to their suggestion. `bio` has no suggestion (not one of the founder's requested fields) and stays founder-only. CLI: `--name`/`--personality`/`--age-range`/`--communication-style`/`--visual-style` all optional on `create-from-proposal`.
  - Live-verified: the real proposal text now shows the full AI-suggested draft (e.g. for a Mexican-market opportunity: `name='Camila Ruiz', age_range='30-40', personality='...', communication_style='...', visual_style='...'`); creating with zero overrides produces an influencer with every suggested field; creating with one override (`--name`) keeps every other field on its suggestion.

## Architecture: End-to-End Business Execution loop — verified gap audit (2026-08-03)

The founder's "ATLAS Founder Directive – End-to-End Business Execution" names the full loop this codebase is meant to eventually realize: Research → Evidence → Success Patterns → Opportunity → Business Assets → Persona → **Brand** → **Shopify/sales channel** → **Landing pages** → **Creatives** → Launch → Measure → Learn → Improve → Scale → Repeat. A full, direct-file-read audit (not inference from naming) against this loop, done before building anything further, found:

- **Credential-blocked, no internal-only progress possible** (each needs a real, external, vendor-specific integration decision, the same class of decision this codebase has always deferred explicitly rather than guessed): **sales channel/Shopify** (no storefront/checkout/listing mechanism exists anywhere; `CommerceProvider` only validates affiliate links), **creative asset generation** (`CreativeAgent` only lets the founder attach an already-produced real file — confirmed zero image/video/audio generation calls exist anywhere), **real publishing** (`ContentPublisher` is a reserved, zero-implementation Protocol; `PublishingGatewayAgent` queues locally and waits for the founder to report a manual post).
- **Buildable now, zero new external dependency**: **Brand creation** (built this session, below), **"identify success patterns"** (still not built — only aggregate win-rate/ROI averages per category exist; nothing compares content style/format/niche combinations), **landing pages** (only marketing-copy text fields exist today, `landing_page_message`/`landing_page_strategy` — the copy-generation half is buildable without new dependencies, hosting a real live page is credential-blocked).

## Architecture: Brand Factory (2026-08-03)

The next step in the founder's stated loop, immediately after Digital Influencer creation. Mirrors the Digital Influencer Factory's exact split and pattern — new package `src/atlas/brand/` (`models.py`/`registry.py`/`factory.py`), built and tested bottom-up (model → registry → draft → suggestion → creation → wiring → CLI), each piece verified in isolation before the next depended on it:

- **`Brand`** (`brand/models.py`) — the company/product identity a Campaign operates under, distinct from a `DigitalInfluencer` (the persona promoting it) and from `Campaign.product_offer` (the raw product/niche name). `BrandRegistry` mirrors `InfluencerRegistry` exactly (`.atlas/brands.json`).
- **`BrandDraft`/`draft_brand_proposal()`** — real, evidence-derived facts only: niche/category/market from the real `AffiliateOpportunity`, `recommended_name` defaulting to the real product name (a plain fact — many real digital businesses use the product name as the brand name — not an invented one). Reuses a newly-extracted shared helper, **`opportunity_ranking.cited_evidence()`**, factored out of `influencer.factory.draft_influencer_proposal()` (which was refactored to use it too, re-verified against its existing test suite) so the evidence-citation logic lives in exactly one place across both Factories.
- **`BrandSuggestion`/`suggest_brand()`** — tagline/visual_identity/voice: the same AI-suggested, deterministic, hash-seeded, clearly-labeled treatment the founder already established for a Digital Influencer's WHO fields, applied consistently here rather than re-litigated. Kept as a structurally separate type from `BrandDraft`, same reasoning as `PersonaSuggestion`/`InfluencerDraft`.
- **`create_brand_from_proposal()`** — fail-closed on approval ordering (same guarantee as the influencer factory), **plus a new distinguishing-marker check** (`TASK_MARKER = "Brand Factory:"`) both factories now use: a single opportunity can justify both a Brand proposal and an Influencer proposal, sharing `category="create_asset"` and the same `source_opportunity_id`, so that pair alone can't tell the two apart — `create_influencer_from_proposal()` was retrofitted with the same marker check for symmetry (`TASK_MARKER = "Digital Influencer Factory:"`), re-verified against its existing tests. Auto-links the new Brand to the real running Campaign for the same goal, when one exists (`Campaign.brand_id` + `campaign.registry.link_brand()`, mirroring `link_destination_url()`).
- **`campaign_advance._missing_brand_task()`** — unlike the influencer gap (only raised on a market mismatch), a Brand proposal is raised **unconditionally** for every real opportunity that reaches a Campaign — the founder's loop treats "create the brand" as a normal step, not a fallback. Non-blocking: the Campaign is already active regardless.
- CLI: `atlas brand create-from-proposal <task_id> [--name] [--tagline] [--visual-identity] [--voice]`, `atlas brand list`, `atlas brand show`. `atlas campaign show` now prints the linked Brand.
- Live-verified end-to-end in a scratch dir: real evidence → a real Campaign **and** a real, evidence-citing Brand proposal both appear in the same tick → `create-from-proposal` correctly refused pre-approval → approved → created with every field defaulting correctly (real name from evidence, AI-suggested tagline/visual identity/voice) → the real running Campaign automatically shows the new Brand linked.

**Extended (2026-08-03, same day) — Logo/Banner**: `Brand` gained `asset_library: list[AssetLibraryEntry]` — reuses `influencer.models.AssetLibraryEntry` rather than a duplicate dataclass. No real logo/banner generation exists anywhere in this codebase (same boundary as Creative Assets); `brand.visual_identity` serves as the real creative brief a founder or designer works from, and `brand.registry.attach_brand_asset()` (mirrors `influencer.registry.attach_asset()`) records the real file once it exists. CLI: `atlas brand asset attach <brand_id> --type logo|banner|image|video --reference <path>`.

## End-to-End Business Execution loop — verified status (2026-08-03, per-step audit)

Founder's named chain: Opportunity Discovery → Product Selection → Supplier Identification → Brand Proposal → Digital Influencer → Logo → Banner → Shopify Store → Landing Page → Creative Assets → UGC Videos → Publishing → Sales Tracking → Learning.

| Step | Status |
|---|---|
| Opportunity Discovery | Real |
| Product Selection | Real, `affiliate` only (`AffiliateOpportunity` stage machine + founder approval) |
| Supplier Identification | **Does not exist** — the only "supplier" concept anywhere is workforce/staffing suppliers in Recruitment, unrelated to a product supplier |
| Brand Proposal | Real |
| Digital Influencer | Real |
| Logo / Banner | Real (asset-library recording — not generation, see above) |
| Shopify Store | **Credential-blocked** — no storefront/checkout mechanism anywhere |
| Landing Page | Half-real — copy-generation exists, hosting a live page doesn't |
| Creative Assets | Founder-attach only — no real generation |
| UGC Videos | **Does not exist** — same root cause as Creative Assets |
| Publishing | **Credential-blocked** — queues locally, founder posts manually |
| Sales Tracking | Real — `Ledger` + `KPIRegistry` + revenue-recording CLI, used in the live pilot |
| Learning | Partial — reallocation exists; "identify success patterns" as a distinct capability doesn't |

**Supplier Identification and Shopify Store are deliberately deferred as a linked pair** — a supplier is only useful with a real place to sell, and Shopify is credential-blocked; building one without the other would be exactly the "isolated feature" the founder's own doctrine warns against. Same reasoning for UGC Videos (blocked on the same missing generation integration as Creative Assets) and Publishing/landing-page-hosting (both need a specific real vendor decision no one has made yet). "Identify success patterns" is the next real, buildable-now, zero-dependency gap.

## Architecture: Digital Influencers as reusable business assets (2026-08-03)

Founder's explicit architecture directive: "Digital Influencers are not marketing assets. They are long-term business assets... The objective is not to create more influencers. The objective is to build valuable reusable digital assets that can generate revenue across multiple business opportunities." `DigitalInfluencer.categories` already supported multiple business models structurally (an open list, never enforced as one-per-influencer) — what was missing was ATLAS ever actually *preferring* reuse over creation.

- **`atlas/brain/influencer_value.py`: `influencer_lifetime_value()`** — real profit (via `cashflow.profit()`, never fabricated) summed across every real Campaign an influencer has ever been part of, any category. None (never 0.0) until at least one campaign has measured profit.
- **`influencer.registry.add_category()`** — extends an existing influencer's real category tags after creation (idempotent). The mechanical move behind reuse.
- **`campaign_advance._find_reusable_influencer()`** — only reached when `rank_influencers()` already found nobody tagged for the category at all (previously: `continue`, campaign creation skipped outright). Searches every active influencer regardless of category, ranks real market matches by `influencer_lifetime_value()` first (an established, already-proven asset beats an unproven one), falls back to raw evidence volume when nothing is measured yet. If a match exists, `add_category()` extends them into the new category and the campaign proceeds using them — the Digital Influencer Factory's creation-proposal path is never even reached.
- Live-verified: an influencer created for `digital_product` was found and reused for a new `affiliate` opportunity in the same real market, her categories genuinely extended (`digital_product, affiliate`), zero new influencer created.

**Real defect found and fixed while building this (2026-08-03, live-demo catch, not a test-only bug)**: `IdentityProfile.nationality` was being set by the Digital Influencer Factory to a human-readable name (`"American"`, from `MARKET_LOCALE`), but every matching site (`prefer_market_match()`, `_missing_market_influencer_task()`, and this new `_find_reusable_influencer()`) compared it against `opportunity.recommended_market` — a raw code (`"US"`). `"American" != "US"`, always, silently — meaning **no influencer ever created through the real Factory path could have been recognized as a market match by any of these three functions**, since the day `nationality` was introduced. Every earlier live demo happened to construct test influencers with a code directly in the `nationality` field ("US"/"DE"), coincidentally masking the bug. Caught only once a demo used the *real* Factory-created shape.
- **Fix**: `IdentityProfile` gained a distinct `market: str` field (the raw code, matched against `opportunity.recommended_market`/`Finding.market`) alongside `nationality` (the human name, display-only now). `create_influencer_from_proposal()` sets both. All three matching sites now compare `identity.market`, never `identity.nationality`. CLI: `influencer create --market` (new) alongside `--nationality` (display-only, help text corrected).
- Every test across `test_ranking.py`/`test_campaign_advance.py`/`test_factory.py` that used `nationality=` as a match-key setup was updated to `market=`; `test_factory.py`'s legitimate display-value assertions (`nationality == "American"`) were left alone and a new `market == "US"` assertion added alongside.

## Architecture: Business Asset Portfolio (2026-08-03)

Founder's directive, one level up from "Digital Influencers are reusable assets": "Digital Influencers are only one type of reusable business asset. ATLAS should maintain a Business Asset Portfolio... Before creating any new asset, ATLAS should first search for an existing reusable asset that already fits the opportunity. If one exists, reuse it... ATLAS should optimize the company's total digital asset portfolio, not individual campaigns."

Built as a thin, read-only view over the real, already-existing registries — no new storage mechanism, no rewrite of `DigitalInfluencer`/`Brand`:

- **`atlas/brain/asset_value.py`** (renamed same day from the influencer-only `influencer_value.py`, once Brand needed the identical measurement — mechanical rename, two import sites, zero behavior change, re-verified against the full existing test suite before continuing): shared `asset_lifetime_value()` core, `influencer_lifetime_value()`, and new `brand_lifetime_value()` (`Campaign.brand_id` linkage, same fail-closed real-profit-only discipline).
- **`campaign_advance._find_reusable_brand()`** — the same "search before create" principle just applied to Influencers now applies to Brands: before proposing a new Brand, search every existing Brand for an exact niche match (a Brand's identity is tied to a specific product, so niche — not market — is the real reuse key), ranked by measured lifetime value on a tie. Wired in: `advance_decision_driven_campaigns()` now tries reuse first, only falling through to `_missing_brand_task()`'s creation proposal when no real match exists. `CEOBrain` gained `self.brands` (a `BrandRegistry`, threaded through the same optional-constructor-param pattern every other registry already uses).
- **`atlas/brain/portfolio.py`**: `PortfolioEntry` (asset_type/asset_id/name/market/business_models/lifetime_value) + `portfolio_entries()` (aggregates every active Influencer and every Brand today — extending to a future asset type is one more loop appended here, not a change to anything existing) + `rank_portfolio()` (real lifetime value descending, unmeasured last). CLI: `atlas portfolio list`.
- Live-verified: a pre-existing Brand for the same niche in a different market was reused (zero new Brand Factory proposal raised) and linked to the new campaign automatically; `atlas portfolio list` showed both the real Influencer and the real Brand, ranked, with real business models and market.

**Honest status of every asset type the founder named**, since only two have a real top-level model/registry today:

| Named asset | Status |
|---|---|
| Digital Influencers | Real top-level asset — model, registry, factory, reuse, lifetime value, portfolio-tracked |
| Brands | Real top-level asset — same, as of this session |
| Logos / Banners | Real, but only as `AssetLibraryEntry` records inside `Brand.asset_library` — not independent, portfolio-tracked assets with their own lifetime value |
| Creative Assets / UGC Videos | Real, but only as `AssetLibraryEntry` records inside `DigitalInfluencer.asset_library` — same limitation |
| Image / Prompt Libraries | Real, but only as `ContentTemplate` entries (`image_prompt`/`video_prompt`/`voice_prompt` kinds) inside `DigitalInfluencer.templates` — not independent |
| Landing Pages | Only marketing-copy text fields exist (`landing_page_message`/`landing_page_strategy`) — no real page artifact of any kind, tracked or not |
| Marketing Strategies | `Campaign.platform_strategy`/`content_strategy`/`cta_strategy` are free text per-campaign — nothing reusable across campaigns |
| Winning Campaigns | `Campaign` itself is real and measured, but nothing treats a high-performing campaign's strategy as a reusable template for a future one |

**Performance-history dimensions the founder asked for beyond lifetime value** (conversion rate, markets/languages/platforms where an asset performs best) have **no real data source anywhere in this codebase yet** — deliberately not fabricated here. `lifetime_value` (real revenue-based profit) is the one dimension genuinely measurable today; a per-market/per-platform performance breakdown would need new, real attribution data this codebase doesn't collect yet, not just new aggregation code.

## Architecture: Success Laws — external intelligence, never a blueprint (2026-08-03)

Founder's standing rule: "Every external source (video, article, course, creator, company, or successful business) should be treated as intelligence, never as a blueprint... ATLAS never copies implementations. ATLAS extracts reusable business intelligence." Gives concrete shape to what was previously just a named, unbuilt gap ("identify success patterns" in the Learning step).

- **`SuccessLaw`** (`brain/models.py`) — `principle` (a generalized, transferable rule) kept structurally separate from `source_description` (what was actually observed), specifically so the record can never collapse into "do what {source} did." `evidence_finding_ids` cites real `Finding`s; `applicable_business_models` is an open list, the founder's own explicit generalization test. No stored `status` field — "evidence-backed" vs. "hypothesis" is derived from `bool(evidence_finding_ids)` at read time, never a separately-set value that could drift out of sync.
- **Deliberately not an automated extraction tool**: no real content-analysis/LLM integration exists anywhere in this codebase to read a video/article and derive a principle on its own — that would be its own separate, credentialed decision, the same class already deferred everywhere else (`ContentPublisher`, `MarketSignalProvider`). What's real and built: a durable, evidence-linked place to *record* a Success Law once the founder (or a future real analysis tool) identifies one.
- `KnowledgeBase` gained `save_success_law()`/`success_laws()`/`get_success_law()` — same store, same file (`knowledge.json`), reusing the existing durable-Intelligence-record concept a SuccessLaw is one synthesis level above a `Finding`, rather than a new parallel store. Tolerates a knowledge.json saved before this existed (no migration).
- CLI: `atlas brain law add <principle> <source_description> [--evidence-finding ID ...] [--model CATEGORY ...]`, `atlas brain law list`. Fail-closed: citing an evidence finding id that doesn't real exist raises, the same `unknown ids` discipline `create_campaign()` already applies to influencer ids.
- Live-verified: a fake evidence-finding id was correctly rejected; a real, evidence-linked law printed `evidence-backed`; a principle recorded with no citations printed `hypothesis` honestly, never presented as equally reliable.

## Architecture: Decision Engine Integration — the reasoning loop (2026-08-03)

Founder's directive: "Now I want to stop expanding isolated capabilities. The next milestone is making ATLAS think... identify the smallest architectural change required." Full audit against the founder's stated 12-step reasoning loop (gather evidence → retrieve/rank Success Laws → identify business models → estimate probability → select path → reuse assets → generate missing assets → execute → measure → learn → update Success Laws/lifetime values) found: **`campaign_advance.advance_decision_driven_campaigns()`, already running every tick, is already most of this loop** — steps 1, 5, 6, 7, 8, 9, 10 are real and connected today (evidence → confidence-ranked opportunity → reuse-before-create for both Influencer and Brand → real Campaign → real measurement). Not a rebuild.

**The two genuinely missing links**: (a) Success Laws were recorded (previous session) but never *retrieved or consulted* by anything — steps 2-3. (b) Nothing feeds a real measured outcome back into a Success Law — step 12. (b) requires a real, unresolved attribution design question (which Success Law "caused" a specific campaign's result?) that isn't honestly answerable by inventing a causal claim — deliberately not built this pass.

**The smallest change for (a)** — additive only, no change to `decide()` or any core, tested control flow:
- **`opportunity_ranking.relevant_success_laws(category, knowledge)`** — retrieves every real `SuccessLaw` whose `applicable_business_models` matches `category` (or is category-general), ranked by real evidence volume (evidence-backed always outranks an unevidenced hypothesis, more citations outranks fewer) — never a fabricated numeric confidence for a principle with no real backing.
- **`explain_opportunity_subject()`** gained one additive dict key (`"success_laws"`) — the existing "why is this opportunity promising" explanation founders already review (`atlas brain opportunities --explain`) now surfaces relevant business intelligence directly in that trail. Confirmed the existing test suite doesn't break from the new key (dict-shape tests check specific keys exist, not that no others do).
- **Deliberately not done yet**: blending Success Laws into the *numeric* confidence score itself (`confidence_score()`/`opportunity_confidence()`). What weight a principle should carry against measured evidence is a real, separate design decision — the same class of explicit, stated choice every other `WEIGHTS` constant in this codebase already is, not something to fold in silently as a side effect of visibility.
- Live-verified: an evidence-backed law ranked above an unevidenced hypothesis in the real explain output; a law scoped to a different business model was correctly excluded from an unrelated category's explanation.

## The first complete, measurable, closed-loop business cycle (2026-08-03)

Founder's directive: "Do not expand the architecture. The next milestone is to complete the first end-to-end measurable business loop... Build the smallest possible architecture required to close this complete loop." This closes the exact gap the previous "Decision Engine Integration" milestone deliberately left open: no honest mechanism fed a real measured outcome back into a Success Law.

**The honest resolution to the causation problem**: ATLAS cannot prove a Success Law *caused* a campaign's result — no evidence supports that claim, and inventing one would violate the same fail-closed discipline every other real-vs-fabricated boundary in this codebase already enforces. What it *can* do honestly is record a real, traceable ASSOCIATION — which laws were relevant at the moment a campaign was created — and aggregate real outcomes against that association, the same "real outcomes, never causation" discipline `confidence.historical_success_score()` already applies at category level.

- **`Campaign.success_law_ids`** (new field) — the real laws `opportunity_ranking.relevant_success_laws()` found relevant, recorded once at campaign-creation time in `campaign_advance.py`, never fabricated or backfilled after the fact.
- **`asset_value.success_law_lifetime_value()`** — real profit summed across every campaign a law was relevant for, reusing the exact same shared `asset_lifetime_value()` core `influencer_lifetime_value()`/`brand_lifetime_value()` already use. Zero new aggregation logic.
- **`asset_value.rank_success_laws_by_track_record()`** — the "improve the next decision" step made concrete: re-ranks a list of laws by real track record, preferring a proven positive outcome over an equal-evidence but unproven or negative one. Stable sort preserves the existing evidence-quality order among laws with no track record yet.
- CLI: `atlas brain law list` now shows each law's real track record; `atlas campaign show` shows which laws were in effect.
- **Live-verified, the full cycle in one real run**: real Finding → real Success Law → real Campaign created with that law genuinely linked (`campaign show` confirmed it) → real revenue/cost recorded against the campaign's goal → the law's real track record updated (`brain law list` showed the real $400 profit) → and, the rigorous proof — deliberately feeding the proven law into `rank_success_laws_by_track_record()` *second* in input order, confirmed it still won: **a real measured outcome demonstrably changed which Success Law ATLAS would prefer next time**, not just in theory.

This is additive throughout: no existing function's control flow, signature meaning, or tested behavior changed — `Campaign` gained one field with a safe default, `create_campaign()` gained one optional param, `asset_value.py` gained two functions built on its existing shared core.

## Closing ATLAS Cash Flow V1 — the real publishing package (2026-08-03)

Founder's directive after the engineering audit: build everything still possible without credentials, so the only remaining action is connecting an account or pressing Approve. Traced the real gap precisely first: `CreativeAgent` (creative brief generation) is entirely tied to the *old* `AffiliateOpportunity.content_package` dict shape — it has zero connection to the real Campaign/`ContentPackage` pipeline the live pilot actually runs on. The new pipeline had no creative-brief step and no landing-page artifact at all, only text copy fields. Three small, additive functions in `influencer/production.py` (no new file, no redesign):

- **`generate_landing_page_html(campaign, package)`** — real, deterministic, escaped static HTML built from already-approved copy (title/description/hook/cta) and the real `destination_url`. Fail-closed: raises if the essential copy or a real link isn't there yet, never renders a fabricated page. Needs no credentials to generate — needs a real hosting account to go *live*, which stays a separate, later step.
- **`generate_campaign_creative_brief(campaign, package, platform)`** — the real Campaign-pipeline equivalent of `CreativeAgent.generate_creative_brief()`'s shot-list, deliberately not reusing that function directly since the two operate on incompatible real data shapes. Still no image/video/audio generation — the real shot list a human or a future real generation provider works from.
- **`assemble_publishing_package(campaign_id, influencer_id, ...)`** — the one real artifact "connecting the account" is the only thing standing between: every real copy field, real attached media, real platforms, the real landing page, and the real creative brief, in one inspectable bundle. Never generates new content itself — pure composition of the three functions above plus `generate_content_package()`. `landing_page_html` is honestly `None` (not fabricated) when the campaign isn't ready yet.
- CLI: `atlas campaign package <campaign_id> <influencer_id> [--export-landing-page PATH]` — live-verified to produce a real, valid, escaped HTML file from a real campaign's real approved copy.

**Explicitly not attempted**: any real external API integration (Digistore24 sales data, any publishing platform, any content-generation provider, any market-signal source). `Digistore24Provider.fetch_recent_sales()`'s own docstring already states why — guessing at a live financial API's auth/endpoint/response shape without verified current documentation is a real risk, not a shortcut, and the same discipline applies to every other credential-blocked connector. These stay honestly unbuilt until the founder supplies real, verified API details to build against — not because they're hard, but because guessing would be worse than leaving them undone.

## ATLAS Ready for Work — the daily operational workflow made real (2026-08-03)

Founder's directive: "I want ATLAS to be able to wake up every day and operate like a real digital company," listing 12 explicit steps. Audited `CEOBrain.tick()` (already running every 30 minutes via the real `ATLAS-Tick` Windows Scheduled Task) against all 12 first, before writing anything — found steps 1-5, 7-10 were already real and wired (scan → `absorb_opportunities`/`advance_opportunity_discovery`; rank → `decide_all`/`confidence_score`; select → prioritizer; reuse assets → `_find_reusable_influencer`/`_find_reusable_brand`; generate missing → the capability-gap Influencer/Brand Factory tasks; wait on approval/credentials → `request_founder_review` + reserved `CommerceProvider`/`ContentPublisher` Protocols; record → `Task.history`/`plan.event_log`; measure → `check_measurement`). Two genuine gaps remained, both closed additively, no redesign:

- **Step 6, "prepare complete campaign packages," wasn't automatic.** `assemble_publishing_package()`/landing-page export existed only as an on-demand CLI command (`atlas campaign package`), never something the daily tick loop itself produced. Fixed by extending `orchestrator._produce_content()`: the moment a step's every publish-readiness requirement is verified (same gate as before, unchanged), it now also calls `generate_landing_page_html()`/`generate_campaign_creative_brief()` (both already-tested, already-built) and writes the real landing page HTML to `.atlas/landing_pages/<campaign_id>_<influencer_id>.html` — reusing the exact functions the CLI command already used, zero new business logic, zero new `ExecutionStep` kind. `landing_page_dir` is threaded through `advance_execution()`/`advance_all_campaign_executions()` as an injectable parameter (default `.atlas/landing_pages`, same convention every registry's default store path already uses) specifically so tests never write into real production `.atlas/` data — caught and fixed a real pollution incident during this work (test runs had written 9 files into the live `.atlas/landing_pages/`; deleted, then fixed the root cause via injection rather than just cleaning up once).
- **Step 12, "Produce a Daily Executive Report," didn't reflect any of this session's other work.** `Reporter.summarize()` predated Opportunity Discovery, Success Laws, the Asset Portfolio, and publishing packages entirely. Extended it with four new, additive, optional-registry-gated sections — `opportunities` (findings this period + real evidence-ranked category/subject standings, reusing `confidence_score()`/`rank_opportunities()` verbatim), `success_laws` (count + real track record via `rank_success_laws_by_track_record()`), `asset_portfolio` (top assets by real lifetime value via `rank_portfolio()`), `publishing_readiness` (packages actually prepared + named blocked reasons, read directly off `ExecutionStep.result`). Every section is a read-only view over already-existing, already-tested functions — no new scoring or ranking logic anywhere. All five new params (`knowledge`/`campaigns`/`influencers`/`brands`/`execution_plans`) default to `None` and degrade to empty sections, so every pre-existing caller/test kept working unchanged; `CEOBrain.review()` now passes the real registries it already holds.
- **Step 11, "Update Success Laws," needed no new code.** There is no mutable "current value" to update — `success_law_lifetime_value()`/`rank_success_laws_by_track_record()` already recompute live from real campaign outcomes on every call, so surfacing them in the report each period *is* the update, the same "nothing is permanently true, recompute fresh" discipline the Decision Engine already relies on. Authoring a brand-new law's principle text stays a founder/analyst judgment call (`atlas brain law add`) — no standing policy authorizes fabricating business-principle prose the way identity/brand creative fields do (an explicit "AI-suggested draft" policy exists for those; none exists for Success Law text), so none was invented here. Documented as a real engineering finding, not coded around.
- **Live-verified, the entire loop in one real `brain.tick()` call**: seeded two independent real findings + one Success Law + one founder-created, publish-ready influencer/campaign in a scratch `.atlas` dir, called `brain.tick()` once (the exact call `ATLAS-Tick` makes every 30 minutes), then `brain.review("daily")` — confirmed the report showed the real ranked opportunity (KetoDNA, market=US), the real Success Law with an honest `null` track record (no campaigns measured yet), the real influencer in the asset portfolio, and `packages_ready: 1` with a real, valid landing-page HTML file on disk — all produced automatically, not via any manual CLI step. Also verified via the real `atlas.exe brain report --period daily` CLI path. Full suite: 806 passing (801 → 806, five new reporter tests).

## Integration 1 (in progress) — Digistore24, the real affiliate workflow (2026-08-03)

Founder's directive: connect Cash Flow V1 to real services one at a time, starting with the affiliate workflow already in use, and verify each with a real end-to-end run before starting the next. `Digistore24Provider.fetch_recent_sales()` was the named, deliberately-unbuilt gap from the earlier engineering audit — this is that gap, built as far as it honestly can be without a real credential.

**What was verified, and how**: Digistore24's official developer docs (`dev.digistore24.com`) are login-gated — every `WebFetch` attempt returned HTTP 403, so their exact field-level response shape could not be first-party confirmed. What IS independently verified via `WebSearch` (real, official doc page titles, not inference): the API is method-in-path (`getUserInfo`, `getPurchase`, `listPurchases`, `getPurchaseTracking`, `createBuyUrl` are real, named methods — not a generic REST resource style). The base URL (`https://www.digistore24.com/api/v1/`) and auth header (`X-DS-API-KEY`) are the best-attested values from independent secondary sources (the header name was independently corroborated twice) — real, worth building against, but explicitly flagged provisional in the code's own docstring until one real authenticated call confirms them.

- **`Digistore24APIError`** (new) — any HTTP error, network failure, or malformed/unexpected JSON response raises this loudly. Deliberately never silently returns `None`/`[]` on a real failure — that would be indistinguishable from "no credential configured," which is exactly the kind of fabricated-looking success this codebase's fail-closed rule exists to prevent. A wrong header name or a changed endpoint will surface immediately as a clear error naming the likely cause, not a silent empty result.
- **`Digistore24Provider._call(method, params)`** — the one real, authenticated `urllib.request` GET call every method goes through (stdlib only, no new dependency — this project has zero third-party runtime dependencies today and that stays true).
- **`verify_connection()`** (new) — calls the real, parameter-free, financial-data-free `getUserInfo` method specifically so the auth assumptions above can be confirmed with the lowest possible real-world risk, before `fetch_recent_sales()` is ever trusted.
- **`fetch_recent_sales()`** — now a real call to `listPurchases`, but deliberately returns the API's raw `data` entries exactly as sent, never renamed/remapped into ATLAS's own field names — this integration hasn't yet seen one real response to know what those fields actually are, and inventing a mapping now would be a fabrication. Normalizing the real shape into ATLAS's internal representation is the natural next increment, once a real response has actually been seen.
- CLI: `atlas affiliate digistore24 verify` (the real, low-risk connection check) and `atlas affiliate digistore24 sales` (prints the real, raw `listPurchases` response for inspection).
- Tests: full mocked-HTTP coverage (auth header sent correctly, URL/method correct, real success path, HTTP error, network error, malformed JSON, missing `data` key) — all against a fake `urllib.request.urlopen`, never a real network call in the suite.

**Verification status — honest, per the founder's own 4-step process**:
1. Build — done.
2. Test — done (mocked; 815/815 suite passing).
3. Verify with a real end-to-end run — **blocked, not yet done.** Needs a real `DIGISTORE24_API_KEY` set as a real environment variable (never pasted into chat — this session never saw or will see the raw key), and one real call to confirm the provisional auth-header/base-URL assumptions actually hold. `atlas affiliate digistore24 verify` is built and ready to be that real call the moment a key is set.
4. Report — this section, plus told to the founder directly: real, low-risk (`getUserInfo`, no financial data) verification is one env var away from being real; `listPurchases`'s exact per-purchase field shape remains unverified until that first real call happens, and normalizing it into ATLAS's internal shape is deliberately deferred until then, not guessed now.

**What this does NOT unblock yet**: `fetch_recent_sales()` being real doesn't mean anything in the tick loop calls it automatically — revenue attribution today still flows through the founder-reported `atlas affiliate revenue record` path, unchanged. Wiring `fetch_recent_sales()` into the actual measurement loop (`_check_measurement`/`kpi_intake`) is a distinct, later increment, appropriately not attempted before the real API shape itself is confirmed.

## Architecture: operator interface (console / REPL / full-screen app)

Added late in the 2026-07-22 session, with no preceding design doc (the first code in this project to ship that way) — all read-only/dispatch-only, no new business logic:

- `src/atlas/repl.py` — `run_repl()`: a persistent line-based console. Every command (`status`, `briefing`, `departments`, `approvals`, `approve <id>`, `reject <id>`, `queue`, `campaigns`, `opportunities`, `kpi`, `warnings`, `activity`, `exit`) calls straight into `atlas.brain.console`'s data functions or `CEOBrain.approve()`/`reject()` — `dispatch()` is presentation and routing only.
- `src/atlas/app.py` — `run_app()`: a full-screen frame around the same `dispatch()`, redrawing a dashboard (`_render()`) after every command. Adds `_ALIASES`, a natural-language phrasing layer mapped onto the exact same commands (e.g. "how are we doing" → `status`) — anything unrecognized falls through to `dispatch()` unchanged, so no command is lost. **This is what `atlas` with no arguments launches.**
- `src/atlas/speech.py` — optional, best-effort local TTS/STT via Windows' built-in `System.Speech` through PowerShell (no external API, no new dependency). `speak()`/`listen()` never raise — any failure (no mic, no recognizer, PowerShell unavailable) degrades silently to "no speech". Text is passed over stdin/captured from stdout, never interpolated into the PowerShell command string, so briefing/command content can never break out of the shell invocation.
- `src/atlas/cli.py` — `atlas console` (one-shot `format_console_view`), and `atlas` with no subcommand at all routes to `run_app()`.

## Explicitly deferred

Named, not silently dropped: real strategic reasoning (an LLM-backed `Planner`/analyst — the protocol accepts it; wiring an actual model call is a separate cost/API-key decision), actually writing new asset/agent files or rewriting workflows on approval (approving marks a `Proposal` `applied`/`approved_pending_manual_creation`; the mechanical change is a separate explicit step), a scheduler/daemon for 24/7 operation (`tick()`/`review()` are designed to be called on any cadence — cron, Task Scheduler, a Claude Code scheduled routine — standing that up is a deployment step), external report delivery (email/Slack), a live web dashboard/HTTP API (the terminal console/app above is not this), a cross-asset event bus, any real external platform integration anywhere (affiliate network APIs, publishing APIs, payment/conversion tracking), and Mission 004 (`docs/FOUNDER_ASSISTED_BUSINESS.md` — a platform-registration workflow + a `founder_explanation` field on `Task`), which was explicitly designed as "design only, no code changes this mission" and was never subsequently implemented — confirmed absent from `src/` as of this rewrite.
